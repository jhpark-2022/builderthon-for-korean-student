# 감사 반영 브리프: 2026-09-18 감사의 제안 전부

Claude Code용. `docs/audit-2026-09-18.md`(페르소나 8종 감사)의 P0·P1·선택 사항을 **전부** 반영한다. `docs/mobile-fix-brief.md`와 겹치는 항목은 그 브리프의 사양을 따르되 이 작업에서 함께 끝낸다(두 번 하지 않는다). 대상은 홈(`/`). `/2026-08`은 손대지 않는다. 카피 키는 지우지 않는다(추가·수정만).

작업 전에 `docs/audit-2026-09-18.md`를 읽는다. 각 항목의 근거 프레임 번호가 거기 있다.

---

## 0. 결정 (사용자, 2026-09-18)

- 주황은 **점으로만**. 제목 2행의 그라데이션은 보라 → 자주에서 끝낸다(8월 문법 브리프 3장의 "보라 → 자주 → 주황"을 이 결정으로 대체). 히어로 날짜 줄은 흰색 볼드, 카운트다운 테두리는 `--border-2`. 화면의 주황은 로고의 나루 점, 배경 등불, 노선도의 현재 위치 점 하나, 카운트다운 옆 점 하나뿐.
- 팔레트 밖 민트·초록은 "열림·상시" 상태 배지 하나(General Mentoring "전 기간 상시")에만 남기고 전부 보라 `#4B3A8C` 토큰으로.

## 1. 히어로 `#top`

1. **등록 버튼.** 라벨 "등록하기" → "등록 준비 중"(en "Registration opens later"). 바로 아래 12px 캡션 "열리면 이 자리에서 알립니다"(en "It opens right here when it does"). 버튼에 `aria-disabled="true"` + `aria-describedby`로 캡션 연결. 비활성 글자 대비 4.5:1 이상(흐림 처리 대신 색으로). 창이 `open`이 되면 라벨·캡션이 자동으로 바뀌는 기존 분기 유지.
2. **나루 한 문장.** 아이브로 "나루의 다음 이벤트" 아래 또는 서술 첫 줄에 "싱가포르에서 시작한 한인 학생 빌더 커뮤니티, 나루의 다음 이벤트입니다."(en 병기). 나루를 모르는 사람이 첫 화면에서 알게. 이 문장은 `TODO: confirm`(나루 정체성 문구는 사용자가 정한다) 주석과 함께.
3. **사진 캡션.** 사진 4장 묶음 아래 한 줄 "제로백 빌더톤 · 2026.08 싱가포르"(11~12px, `text-white/50`). 데스크톱은 호버 시 각 사진 하단에 "Day 1" 같은 캡션 슬라이드(`eventHero.photos[].alt`에서), 모바일은 정적 한 줄만.
4. **카운트다운.** 모바일 브리프 2장대로 한 줄(초 삭제, SGT 삭제, 폰에서 사진 위). 컨테이너에 `aria-label="크로싱 서울까지 N일 N시간"` 하나, 자식의 `aria-live` 제거. 테두리 주황 → `--border-2`. 옆에 주황 점 하나(상태 표시)만 허용. 폰 라벨에 요일 보강 "81일 · 12.10 목".
5. **주황 정리.** "CROSSING SEOUL" 그라데이션 `--purple → --plum`. 날짜 줄 흰색 볼드.

## 2. 헤더

1. 모바일: 아래로 스크롤하면 로고 줄(52px) 접고 칩 레일(48px)만, 위로 스크롤하면 복귀. `html { scroll-padding-top }`를 헤더 실높이로. 8월의 `useScrollDirection` 재사용.
2. 칩 레일 우측 24px 페이드 마스크(`mask-image: linear-gradient(to right, #000 88%, transparent)`), `snap-x`, 활성 칩 `scrollIntoView({ inline: "center", block: "nearest" })`. 칩 높이 44px.
3. EN/KR: 현재 언어에 밑줄 + 굵기, `aria-current="true"`, 각 링크에 `lang`.
4. 배경 정지 토글을 헤더 우측에도 둔다(푸터 것은 유지). 데스크톱 푸터에 토글이 없으면 추가(감사에서 "없는 것으로 보임", 확인 후). 선택은 `localStorage`, `prefers-reduced-motion`이면 초기 정지.

## 3. 프로그램 `#december`

1. 모바일 브리프 1장 전부(워크샵을 Day 카드 안 한 줄로, 참여 플로우 숨김, 아쉬웠던 넷 1열, Day 본문 `line-clamp-2`, 안내 두 줄 합치기). 추가로 **Day 카드를 폰에서 아코디언**(제목+날짜 56px 행, 탭하면 펼침, 첫 카드만 기본 펼침). 목표 챕터 높이 폰 ≤ 2,400px.
2. **데스크톱 노선도 확인.** 노선도가 데스크톱에서 렌더되는지 본다. 렌더되면 참여 플로우 스트립(등록 → 팀 본딩 → 닷새 → 결과 공유회)을 데스크톱에서도 삭제한다(같은 시간축 두 번). 렌더되지 않으면 노선도를 복구하고 플로우를 삭제한다. 어느 쪽이든 결과는 "노선도 하나".
3. **강조색 통일.** Day 카드의 "→ 그날의 한 줄"은 흰색 볼드 하나. BEFORE 노랑, Day 1·2 민트, Day 3 분홍 전부 제거. "★ 제출" 칩만 자주 `#9A5A82`. 나머지 칩은 `--border-2` 외곽선 + `text-white/80`.
4. General Mentoring 상자: 초록 테두리는 유지(유일한 "상시" 배지), 안의 규칙 넷은 흰색.
5. **첫 이벤트 문장 통일.** `notSequel`을 "나루의 두 번째 이벤트입니다. 제로백 빌더톤의 속편은 아니고, 그 8일에서 나온 변하지 않는 두 개를 그대로 잇습니다."로. 8월 챕터의 "나루의 첫 이벤트"와 짝이 맞게.
6. **노선도 현재 위치 점**(선택 사항 채택): 주황 점 하나가 뷰포트에 있는 Day 카드 위치로 노선을 따라 이동(200ms), 제출일 별은 지날 때 한 번 밝아짐. `prefers-reduced-motion`이면 정적. 점은 하나만.

## 4. 얻는 것 `#gains`

1. 카드 높이를 내용에 맞추고 최소 높이만 통일(데스크톱). 폰은 1열 리스트, 행 64px, 번호 필 왼쪽.
2. 번호 배지 01~05 민트 → 보라 토큰. 아이브로 "참가 혜택" 민트 채움 → 보라 외곽선.
3. 각 카드 아래 8월 근거 한 줄(선택 사항 채택. 사용자의 "제목만" 결정과 충돌하므로 **주석 처리한 채 키만 추가**하고 화면에는 그리지 않는다. `gains.items[].evidence`에 값을 넣어 두되 렌더는 `TODO: confirm`). 지어내지 않는다: 실명 기업 → "8월 코드프레소 출제" / 멘토 → "8월 11명" / 앞에서 증명 → "8월 21팀 발표" / 무순위 어워드 → "8월 4부문 10팀" / 국경 너머의 동료 → 근거 없음(비움).

## 5. 8월이 남긴 것 `#record`

1. **숫자 깔때기.** 같은 크기 타일 5개 → 스텝 차트 한 줄(74 → 59 → 25 → 21 → 9, 막대 높이가 줄어드는 5단) + "9팀 · 출제사에 직접 자료 요청 · 시키지 않았습니다"를 아래 큰 숫자로 따로. 폰은 세로 스텝. 3+2 그리드와 높이 불일치 문제가 함께 사라진다. `dataviz` 원칙: 축 없이 값 라벨만, 색은 보라 단색에 마지막 단만 자주.
2. **사람.** 모바일 브리프 3장의 `RecordTabs`(멘토 / 연사와 피드백 패널, 링크드인) 넣기.
3. 언론 줄 제목 `line-clamp-2` + `title`.
4. 챕터 끝, 아카이브 버튼 옆에 텍스트 링크 "8월에 오셨던 분은 →"(`#join-alumni` 앵커, 알럼 띠에 id 부여).
5. 숫자 카운트업(선택 사항 채택): 뷰포트 진입 시 0 → 값 600ms, 한 번만, `prefers-reduced-motion`이면 생략.

## 6. 나루 `#naru`

1. 폰 높이 3,674 → 목표 2,200. 인장 260px → 160px. 코어 판 둘의 "그래서 지키는 것"은 폰에서 접힘(펼치기 버튼). 3줄 넘는 문단은 폰에서 왼쪽 정렬. 데스크톱 판형은 그대로(사용자가 정한 자리).
2. 태그라인 2행 그라데이션 `--purple → --plum`(주황 제거). 코어 제목 앞 나루 점은 유지(주황 점 허용 목록).
3. 원형 인장은 보조 마크로: 크기 160px(폰), 220px(데스크톱), 아래에 "싱가포르 한인 학생 빌더 커뮤니티" 아이브로는 그대로. 헤더 워드마크와의 관계는 로고 가이드가 정본이라 형태는 바꾸지 않는다.
4. "하지 않는 것" 불릿 민트 → 보라.

## 7. 학생회와 기업 `#how` · 함께 `#join`

1. 기업 카드에 **얻는 것** 두 줄 추가(`join.cards[company].gets`): "8월 출제사는 9팀에게 직접 자료 요청을 받았고, CNA와 The Straits Times에 실렸습니다." + "문제와 데이터를 여는 회사가 참가자를 가장 먼저 만납니다." 8월 사실만. `#how` 후원 카드의 "법인격 없음, 돈 직접 안 받음"은 그대로 두되 순서를 얻는 것 뒤로.
2. 보조 버튼 크기 1종. 함께 챕터의 "메일로 문의" 넷은 텍스트 링크(44px 히트 영역)로 내리고, 알럼 띠의 "메일로 보내기"만 외곽선 버튼.
3. 알럼 띠 테두리 민트 → 보라. 아이브로 "함께하는 길" 민트 채움 → 보라 외곽선. 3층 카드의 "운영진으로 함께하기 →" 등 텍스트 링크 셋은 높이 44px.
4. 알럼 띠에 `id="join-alumni"`.

## 8. 브랜드 토큰 (한 번에)

- `Eyebrow`: 챕터 아이브로는 **보라 외곽선 1종**. `orange`·`emerald` 변형은 남기되 챕터 아이브로에서는 쓰지 않는다(주황 글자색 아이브로 "다음 이벤트 크로싱 서울"도 보라로).
- 번호 배지(`BenefitCard`, 아쉬웠던 넷, 얻는 것, 함께): 보라 토큰 하나.
- 초록 허용 목록: General Mentoring "전 기간 상시" 배지 하나. 그 외 `text-emerald-*`, `border-emerald-*`, `bg-emerald-*`를 홈 컴포넌트에서 제거(`grep`으로 0건, 8월 `Journey.tsx`는 제외).
- 주황 허용 목록: 로고 나루 점, 배경 등불, 노선도 현재 위치 점, 카운트다운 옆 점, `#naru` 코어 제목 앞 점. `grep -n "orange\|#EE8A4F\|F2B183"`의 홈 쪽 결과가 이 다섯 자리뿐이어야 한다.
- 영문 라벨 규칙: 이벤트 챕터의 Discovery/Build/Refine/Pitch와 그룹 챕터의 ORGANISER/HOST/SPONSOR는 둘 다 "한글 주 + 영문 소문자 보조" 형식으로 통일(대문자 자간 라벨은 아이브로에만).

## 9. 접근성·구현

1. 모바일 브리프 4·5장(12px 하한, 44px 히트 영역) 그대로.
2. 카운트다운 `aria-live` 제거(1장 4).
3. 히어로 사진 `sizes="(max-width: 640px) 45vw, 230px"`, 위 두 장 `priority`, `width/height` 실제 비율. `src` 없는 `<img>` 원인 제거(`{src ? <Image/> : null}`). 모바일 로드의 502 리소스 URL을 찾아 원인 기록(`/_next/image` 실패면 해당 이미지 `unoptimized` 또는 정적 import).
4. `html { scroll-behavior: smooth }` 제거, 앵커 클릭 핸들러에서 `scrollIntoView({ behavior: "smooth" })`, `prefers-reduced-motion`이면 `auto`.
5. 메타: `title.default` "크로싱 서울 2026 | 나루 NARU", 로케일별 description, `alternates.languages { ko, en }`, `og:locale`을 로케일에 맞게.
6. 섹션 래퍼 `content-visibility: auto; contain-intrinsic-size: 0 900px`(히어로 제외). 폰 `setPixelRatio(min(dpr, 1.5))`, `document.hidden`이면 렌더 루프 정지.
7. Pretendard `next/font/local` preload 확인(이미 그렇다면 체인지로그에 한 줄).

## 10. 하지 않는 것

- 가입 폼, 오픈채팅 재개, 미정 항목(장소·일정표·출제사·멘토·등록일) 채우기.
- 얻는 것 카드에 본문 그리기(키만 추가, 4장 3).
- `/2026-08` 변경. 데스크톱 `#naru` 판형 변경. 로고 형태 변경.
- 아쉬웠던 넷, 하지 않는 것, 미정 목록, 언론 카드, 비활성 등록 버튼에 애니메이션·재치 있는 문구.

## 11. 검증

1. `npx tsc --noEmit`, `npm run build`, 콘솔 오류 0, 가로 넘침 0(390, 1440).
2. **색 검사**: 홈 컴포넌트(`components/home`, `components/shared`, `components/crossing`, `components/ui`)에서 `emerald`가 General Mentoring 배지 한 곳, `orange|#EE8A4F|F2B183`가 8장의 허용 다섯 자리뿐. `grep` 결과를 체인지로그에.
3. **길이**: 폰 합계 ≤ 10,800, `#december` ≤ 2,400, `#naru` ≤ 2,200. 데스크톱 ≤ 9,300. 챕터별 표.
4. 12px 미만 텍스트 0, 44px 미만 히트 영역 0(스킵 링크 제외).
5. 1차 CTA는 히어로 하나. 보조 버튼 크기 1종(`grep`으로 클래스 변형 수 확인).
6. 스크린샷 `.shots/naru/audit-fix/`: 1440·390 × ko·en 전체 프레임(대기 2.5초, `scroll-behavior` 제거 뒤). 히어로 캡션, 등록 준비 중 버튼과 캡션, 깔때기 차트, 노선도 점 이동(연속 3프레임), 접힌 모바일 헤더가 보여야 한다.
7. 캔버스만 찍은 폰 9프레임(모바일 브리프 9장 8번)도 함께.
8. `/2026-08` 전후 픽셀 diff 0.
9. 체인지로그 `docs/changelogs/changelog-september-XX-2026-audit-fix.md`: 감사 항목 번호 ↔ 반영 커밋 표, 확인 필요였던 둘(데스크톱 노선도, 데스크톱 정지 토글)의 실제 상태, TODO: confirm(나루 한 문장, 얻는 것 근거 렌더 여부). 커밋 순서: `fix(brand): 주황을 점으로, 민트를 보라로` → `feat(web): 등록 준비 중 캡션, 나루 한 문장, 사진 캡션` → `fix(mobile): 헤더 접기, 프로그램 아코디언, 나루 축소` → `feat(web): 8월 숫자 깔때기와 사람, 알럼 앵커, 기업 얻는 것` → `fix(a11y): 카운트다운 aria, 언어 토글, 히트 영역, 글자 하한` → `fix(web): 이미지 sizes, 메타, scroll-behavior`. `main` 푸시 전에 1~8.
