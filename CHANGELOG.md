# CHANGELOG

이 파일이 유일한 체인지로그입니다.
새 항목은 맨 위(가장 최근)에 추가하고, docs/changelogs/에 새 파일을 만들지 않습니다.
2026-09-24 이전 항목의 원문은 아래 각 항목의 git 경로로 볼 수 있습니다.

## 목차

### 2026-09

- [2026-09-26 페이지 맨 아래에서 빛을 거둔다](#2026-09-26-end-fade)
- [2026-09-26 싱가포르로 건너는 시점을 앞당긴다](#2026-09-26-morph-earlier)
- [2026-09-26 블록을 한 축에, 폭은 둘로](#2026-09-26-one-axis)
- [2026-09-25 홈 가독성: 읽기 판, 정렬과 라벨, 구체적인 한 줄](#2026-09-25-readability)
- [2026-09-24 크로싱 서울 일정을 12월 18~22일로](#2026-09-24-crossing-dates)
- [2026-09-24 폰에서 싱가포르의 빛이 보이게](#changelog-september-24-2026-background-portrait-light-motion)
- [2026-09-23 보안 정리](#changelog-september-23-2026-security)
- [2026-09-23 싱가포르 빛의 폭과 속도 상한](#changelog-september-23-2026-background-singapore-light-speed)
- [2026-09-23 형상을 챕터에 맞춘다](#changelog-september-23-2026-background-chapter-map)
- [2026-09-23 서울의 존재감](#changelog-september-23-2026-seoul-presence)
- [2026-09-23 배경의 시계를 하나로](#changelog-september-23-2026-background-one-clock)
- [2026-09-20 히어로 스크롤 효과를 걷는다](#changelog-september-20-2026-hero-recede-removed)
- [2026-09-20 히어로 효과의 대상을 제목 묶음으로 좁힌다](#changelog-september-20-2026-hero-recede-fix)
- [2026-09-20 크로싱 서울 일정을 PDF대로](#changelog-september-20-2026-schedule)
- [2026-09-20 상자를 줄이고 목록으로 옮긴다](#changelog-september-20-2026-presentation)
- [2026-09-19 싱가포르 윤곽을 실제 해안선에서](#2026-09-19-background-singapore-detail)
- [2026-09-19 파문 위상을 적분한다](#2026-09-19-background-ring-phase)
- [2026-09-19 #naru에서 서울이 싱가포르로 건넌다](#2026-09-19-background-seoul-to-singapore)
- [2026-09-19 데스크톱에서 서울 윤곽이 보이게](#2026-09-19-seoul-desktop)
- [2026-09-19 세로 화면 파문의 속도](#2026-09-19-background-ring-speed)
- [2026-09-19 세로 화면 배경을 가로 화면에 맞춘다](#2026-09-19-background-portrait-parity)
- [2026-09-19 주소를 naru-crossing-seoul로 옮긴다](#2026-09-19-new-url)
- [2026-09-19 폰 배경을 데스크톱과 같은 순서로](#2026-09-19-mobile-background)
- [2026-09-19 스크롤 리빌, 카운트다운, 매니페스토 줄 정리](#2026-09-19-reveal-and-countdown)
- [2026-09-19 공유 카드를 다시 만든다](#2026-09-19-share-card)
- [2026-09-19 영문을 짧게 줄인다](#2026-09-19-en-shorter)
- [2026-09-19 8월 페이지의 마지막 화면을 12월과 잇는다](#2026-09-19-august-to-december)
- [2026-09-19 마지막 챕터가 묻는 질문을 왜로 바꾼다](#2026-09-19-why)
- [2026-09-19 "나루가 하지 않는 것" 블록을 내린다](#changelog-september-19-2026-notdoing-removed)
- [2026-09-19 1일차: 트랙은 미리 고르고, 팀은 현장에서 맺는다](#changelog-september-19-2026-day1-matching)
- [2026-09-19 얻는 것: 다섯 개에 설명을 단다](#changelog-september-19-2026-gains)
- [2026-09-18 after: 끝나면 할 일 세 줄](#changelog-september-18-2026-after)
- [2026-09-18 감사 반영: 페르소나 8종 감사의 P0, P1, 선택 사항 전부](#changelog-september-18-2026-audit-fix)
- [2026-09-18 Supabase: 크로싱 서울 등록 표를 새로 만들고 12월에 연결](#changelog-september-18-2026-crossing-registration)
- [2026-09-18 나루와 학생회와 기업을 한 챕터로](#changelog-september-18-2026-merge-how-into-naru)
- [2026-09-18 모바일 수정: 폰에서 16.5화면을 줄인다](#changelog-september-18-2026-mobile-fix)
- [2026-09-18 미정 목록 제거, 카운트다운 두 줄](#changelog-september-18-2026-hero-countdown-tbd)
- [2026-09-18 어떻게 일하는가: 두 문서에서 다시 짧게](#changelog-september-18-2026-how-copy)
- [2026-09-18 배경: 스크롤하면 서울이 떠오른다](#changelog-september-18-2026-bg-seoul-watermark)
- [2026-09-17 배경: 강과 등불로 복귀](#changelog-september-17-2026-bg-water-return)
- [2026-09-17 홈 흐름 재배치: 히어로 아래에 사진 대신 답을 둔다](#changelog-september-17-2026-home-flow2)
- [2026-09-17 히어로: 형상 대신 행사 사진 넷](#changelog-september-17-2026-hero-photos)
- [2026-09-17 배경 수정: 형상을 첫 화면 한가운데로, 배경을 다시 살아 있게](#changelog-september-17-2026-bg-fix)
- [2026-09-17 오픈채팅 문 닫기](#changelog-september-17-2026-openchat-off)
- [2026-09-17 히어로 형상 띠, 언론 줄 정렬, 12월 카피](#changelog-september-17-2026-hero-band-copy)
- [2026-09-17 8월의 기록: 언론 줄](#changelog-september-17-2026-record-press)
- [2026-09-17 배경: 기슭의 입자를 싱가포르와 서울 형상으로](#changelog-september-17-2026-bg-shapes)
- [2026-09-17 배경: 건너는 점들](#changelog-september-17-2026-bg-crossing)
- [2026-09-17 나루 홈을 8월 사이트의 디자인 문법으로](#changelog-september-17-2026-august-grammar)
- [2026-09-17 홈의 흐름: 12월을 앞으로, 행동을 제목 아래로](#changelog-september-17-2026-flow)
- [2026-09-16 코어만 남깁니다](#changelog-september-16-2026-essence-only)
- [2026-09-15 크로싱 서울 포지션 반영](#changelog-september-15-2026-crossing-seoul-positioning)
- [2026-09-15 나루 런칭 홈, 제로백 빌더톤을 /2026-08 기록으로](#changelog-september-15-2026-naru-launch)

### 2026-08

- [2026-08-12 Day 2 팀 빌딩 타임 신설, 맞춤 세션 추천 제거](#changelog-august-12-2026-day2-team-building)
- [2026-08-11 FAQ 16 → 14문항: 중복 정리 + 질문 톤 소프트닝](#changelog-august-11-2026-faq-trim-question-tone)
- [2026-08-10 Day 8 트랙 발표 두 슬롯: 문안 통일 + 시간 구성 헤지](#changelog-august-10-2026-day8-track-slots-unify)
- [2026-08-10 노선도와 데이 카드 의미 체계 동기화](#changelog-august-10-2026-daycard-routemap-sync)
- [2026-08-10 노선도 멘토링: 연속 선에서 점 마커로](#changelog-august-10-2026-routemap-mentoring-dots)
- [2026-08-10 히어로 주 CTA를 등록으로](#changelog-august-10-2026-hero-cta-register)
- [2026-08-10 모바일 폴리시 4건](#changelog-august-10-2026-mobile-polish)
- [2026-08-04 모바일 모달 스크롤 격리](#changelog-august-04-2026-modal-scroll-lock)
- [2026-08-04 선택일 카드에 '올 이유' 한 줄](#changelog-august-04-2026-why-stop-lines)
- [2026-08-04 심사위원 질문 인용 풀어쓰기](#changelog-august-04-2026-prompt-question-clarify)
- [2026-08-04 EN 카피 미세 수정 2건 + Day 1 시간 정정](#changelog-august-04-2026-en-copy-nits)
- [2026-08-03 브랜드부스트 굿즈 확정](#changelog-august-03-2026-goods-brandboost)
- [2026-08-03 Day 5 → 네트워킹 데이](#changelog-august-03-2026-day5-networking-pivot)
- [2026-08-03 Day 1 장소 확정, SMU YPHSL → The Foundry](#changelog-august-03-2026-day1-venue-foundry)
- [2026-08-03 혜택 04 또래 네트워킹](#changelog-august-03-2026-benefits-peer-networking)
- [2026-08-03 데스크톱 내비 브레이크포인트 `lg` → `xl`](#changelog-august-03-2026-nav-breakpoint)
- [2026-08-03 확정 파트너 스트립, 티어별 고정 박스](#changelog-august-03-2026-partner-logo-uniform)
- [2026-08-03 mobile partner strip to static](#changelog-august-03-2026-partner-strip-static)
- [2026-08-03 horizontal overflow](#changelog-august-03-2026-horizontal-overflow)
- [2026-08-03 mobile audit fixes](#changelog-august-03-2026-mobile-audit-fixes)
- [2026-08-03 정거장 원칙](#changelog-august-03-2026-station-principle)
- [2026-08-02 background consistency](#changelog-august-02-2026-background-consistency)
- [2026-08-02 mobile: open-chat CTA restored](#changelog-august-02-2026-mobile-openchat-cta)
- [2026-08-02 flow strips: mobile arrow direction and box widths](#changelog-august-02-2026-flow-strip-mobile-arrows)
- [2026-08-02 mobile chrome: auto-hide + slimming](#changelog-august-02-2026-mobile-chrome-autohide)
- [2026-08-01 mentoring: two theme groups](#changelog-august-01-2026-mentoring-two-groups)
- [2026-08-01 mentoring: stage → mentor highlight](#changelog-august-01-2026-mentor-stage-highlight)
- [2026-08-01 final-output & brief framing](#changelog-august-01-2026-output-framing)

### 2026-07

- [2026-07-22 등록 CTA 위계, 확정 파트너 스트립, 카카오톡 전환](#changelog-july-22-2026-cta-hierarchy-partner-strip)
- [2026-07-21 섹션 정리 + 파트너 월 덱 싱크](#changelog-july-21-2026-section-trim-partner-sync)
- [2026-07-21 덱 → 웹사이트 콘텐츠 싱크 2차](#changelog-july-21-2026-deck-content-sync-2)
- [2026-07-20 Supabase 등록 백엔드](#changelog-july-20-2026-supabase-backend)
- [2026-07-18 ?reset=1 QA helper](#changelog-july-18-2026-reset-helper)
- [2026-07-18 solo-matching AI-type attach](#changelog-july-18-2026-solo-matching-ai-type)
- [2026-07-18 team registration](#changelog-july-18-2026-team-registration)
- [2026-07-18 register flow](#changelog-july-18-2026-register-flow)
- [2026-07-18 deck content sync](#changelog-july-18-2026-deck-content-sync)
- [2026-07-15 quiz phase 10](#changelog-july-15-2026-quiz-v10)
- [2026-07-15 quiz phase 9](#changelog-july-15-2026-quiz-v9)
- [2026-07-13 quiz phase 8](#changelog-july-13-2026-quiz-v8)
- [2026-07-13 quiz phase 7](#changelog-july-13-2026-quiz-v7)
- [2026-07-13 quiz phase 6](#changelog-july-13-2026-quiz-v6)
- [2026-07-13 quiz phase 5](#changelog-july-13-2026-quiz-v5)
- [2026-07-13 quiz phase 4](#changelog-july-13-2026-quiz-v4)
- [2026-07-12 quiz phase 3](#changelog-july-12-2026-quiz-v3)
- [2026-07-12 quiz phase 2](#changelog-july-12-2026-quiz-v2)
- [2026-07-12 AI personality test (`/quiz`) improvement pass](#changelog-july-12-2026)

### 2026-06

- [2026-06-15 Design-revamp polish cycle](#changelog-june-15-2026)

---

<a id="2026-09-26-end-fade"></a>
## 2026-09-26 페이지 맨 아래에서 빛을 거둔다
- 범위: `lib/background/shaders/water.ts`(`uEnd`), `water/WaterSurface.ts`(`setEnd`), `scene/BackgroundScene.ts`(`#closing` 앵커, 구간 6).
- 한 것: 맨 아래(매니페스토 버튼 아래, 푸터)에 나루 점의 주황 번짐과 파문이 남아 있었습니다. 사용자 요청으로 거둡니다(DECIDED 2026-09-26).
  푸터 상단이 화면 아래 끝에 들어올 때부터 0.4화면 동안 깊은 물 층(점, 번짐, 파문, 물살)을 0으로. 하늘 그라데이션과 비네트는 남깁니다.
- 검증: 주황 번짐 픽셀 문서 끝 1440×900 2,595 → 132, 390×844 1,722 → 58(남은 것은 푸터 로고의 점). #join 중간은 전과 같음.
- 커밋: 9d1654a

<a id="2026-09-26-morph-earlier"></a>
## 2026-09-26 싱가포르로 건너는 시점을 앞당긴다
- 범위: `lib/background/config.ts`(`stages.morph`), `scene/BackgroundScene.ts`(주석). 형상, 점 크기, 밝기, 파문은 그대로.
- 한 것: #gains와 #naru 사이 이음매에서 서울이 아니라 싱가포르가 보이게 했습니다(DECIDED 2026-09-26, 사용자). 전에는 #naru 로고가 보일 때 건너기가 10%였습니다.
  morph를 { startVh 0.5, spanVh 1.5 }에서 { 1.2, 1.0 }으로. #gains를 읽는 동안 건너고 #naru 상단 0.2화면 앞에서 끝납니다.
  챕터 지도 브리프(9월 23일)의 "startVh 0.5를 바꾸지 마세요"를 사용자 요청으로 뒤집었습니다. 길이를 줄인 것은 서울 구간이 기준 1.6화면 아래로 내려가지 않게 하려는 것입니다.
- 검증: 서울이 설 자리 1920×1080 1.81, 1440×1100 1.75, 1440×900 3.01, 390×844 4.4화면. 1000×505, 1440×900, 390×844 이음매 캡처에서 싱가포르 윤곽.
- 커밋: eedfb42

<a id="2026-09-26-one-axis"></a>
## 2026-09-26 블록을 한 축에, 폭은 둘로
- 범위: `components/home/NaruHome.tsx`, `app/globals.css`(`.reading-plate--read`). 카피, 글자 크기, 색, 배경, 판의 모양은 그대로.
- 한 것: 9월 25일 `BODY_AXIS`가 글의 정렬과 블록의 위치를 섞어 본문을 넓은 기둥 왼쪽 끝에 붙였고, 1440에서 축이 셋(가운데, 144, 216)이었습니다(사용자: "뒤죽박죽").
  모든 블록을 가운데 축에 두고 글은 그 안에서 왼쪽 정렬합니다(DECIDED 2026-09-26). 폭은 READ(max-w-3xl)와 WIDE(max-w-5xl, 셋 이상 가로) 둘.
  루트가 18px이라 READ 864px, WIDE 1152px입니다. 챕터 h2(H2 토큰)도 사용자 요청으로 READ 폭에 맞췄습니다(줄 수 그대로).
- 검증: 가운데에서 벗어난 블록 1440/1280/1024 모두 11 → 0. 왼쪽 끝 1440에서 288(READ), 144(WIDE) 둘뿐.
  max-w는 3xl과 5xl 둘뿐. 폰 390×844 전체 캡처 픽셀 차이 0. 판 밖 글 0, 잔점 중앙값 0, 흰 본문 최저 대비 8.95:1.
- 브리프: docs/one-axis-brief.md
- 커밋: 653fc91, 2258e07

<a id="2026-09-25-readability"></a>
## 2026-09-25 홈 가독성: 읽기 판, 정렬과 라벨, 구체적인 한 줄
- 범위: `components/home/NaruHome.tsx`, `app/globals.css`(`.reading-plate`), `data/naru.ts`(구체적인 한 줄 다섯). 배경, `/2026-08`, `/quiz`는 그대로.
- 한 것: 12월 홈이 8월보다 읽기 어려운 이유가 글자 뒤의 점 잡음이라, 네 챕터 본문 뒤에 흐림 + 반투명 막(카드 아님, 챕터당 하나)을 깔았습니다. 흐림은 deviceMemory 4GB 이하에서 뺍니다.
  가운데 정렬은 챕터 머리(알약 + h2 + 문단 하나)에만, 본문은 max-w-5xl 왼쪽 한 축. 라벨은 알약과 소제목(18px semibold) 둘, 한글 넓은 자간 0곳.
  추상적인 문장 옆 구체적인 한 줄은 사용자가 후보 표를 승인한 대로입니다(DECIDED 2026-09-25). 형상을 비키는 셰이더 변경(브리프 3장)은 검증 1을 통과해 하지 않았습니다.
- 검증: 본문 뒤 잔점 중앙값 1440 13,313 → 0, 390 3,532 → 0. 가운데 정렬 39.7% → 2.8%. 흰 본문 최저 대비 3.84 → 8.78:1.
  4× 스로틀 16.7ms 초과 프레임 38.8% → 38.6~39.8%. /quiz 픽셀 차이 0, /2026-08은 HTML 동일(빌드 ID 제외).
- 브리프: docs/readability-brief.md
- 커밋: fcb611c, f1fbc16, 08493f5

<a id="2026-09-24-crossing-dates"></a>
## 2026-09-24 크로싱 서울 일정을 12월 18~22일로
- 범위: `lib/naruDates.ts`, `app/layout.tsx`(메타 설명, 공유 카드 제목), `app/opengraph-image.tsx`, `data/dictionary.ts`(/2026-08 마무리 두 줄)
- 한 것: 시작일 12/10 → 12/18(금), 종료일 12/14 → 12/22(화). 학교 기말고사가 12/14~18에 몰리고 시험 일정과 조금이라도 겹치면 참여가 급감한다는 경험칙에 따라 옮겼습니다(DECIDED 2026-09-24, 사용자).
  18일에 어젠다(Day 0), 19~20일 주말에 실질 활동, 21~22일 마무리. Day 0~4 구성은 그대로이고 날짜만 8일 뒤로. 날짜 카드와 카운트다운은 `naruDates`에서 셉니다.
- 검증: 홈 날짜 줄 "2026년 12월 18일부터 22일까지, 서울.", 데이 카드 12.18 금 ~ 12.22 화, 카운트다운 12/18 0시(KST, SGT) 기준, `/2026-08`에 "12월 10일" 0건.
- 커밋: 5a95a25

<a id="changelog-september-24-2026-background-portrait-light-motion"></a>
## 2026-09-24 폰에서 싱가포르의 빛이 보이게
- 범위: `lib/background/config.ts`, `scene/BackgroundScene.ts`, `shaders/water.ts`, `water/WaterSurface.ts`. 가로 화면의 움직임과 파문은 바꾸지 않음.
- 한 것: 점의 가로 이동을 셰이더에서 `BackgroundScene`으로 옮겼습니다(`uLightDx`). `uStage4`, `uSweep`는 이 식에서만 쓰여 지웠습니다.
  세로 화면은 `LIGHT_PORTRAIT`(폭 0.26, 한 바퀴 2.6화면, 초당 0.07바퀴 상한)를 씁니다. `LIGHT_SWEEP.portrait`는 쓰지 않고 남겨 두었습니다.
- 검증: 데스크톱 옛 식과 차이 최대 8e-8, 캡처 차이 최대 1/255. 폰 속도 중앙값 30.0px/s(전 10.0), 플릭 최고 49.2px/s, 동쪽 끝이 76%를 약 2%p 넘음.
- 브리프: docs/background-portrait-light-motion-brief.md
- 원문: `git show bf718be:docs/changelogs/changelog-september-24-2026-background-portrait-light-motion.md`

<a id="changelog-september-23-2026-security"></a>
## 2026-09-23 보안 정리
- 범위: 12월 신청, 등록 로그, 브리프.
- 한 것: 12월 신청에 봇 확인을 더하고, 등록 로그와 브리프에서 개인정보를 줄였습니다.
- 원문: `git show bf718be:docs/changelogs/changelog-september-23-2026-security.md`

<a id="changelog-september-23-2026-background-singapore-light-speed"></a>
## 2026-09-23 싱가포르 빛의 폭과 속도 상한
- 범위: `lib/background/config.ts`, `scene/BackgroundScene.ts`, `shaders/water.ts`, `water/WaterSurface.ts`
- 한 것: 빛의 폭을 `LIGHT_SWEEP` 0.12(가로) / 0.15(세로)로 두고 셰이더 상수를 `uSweep` 유니폼으로 옮겼습니다. `s4Eased`의 초당 변화에 상한 `LIGHT_MAX_RATE` 0.05를 걸었습니다.
  브리프에 없던 것: 셰이더 uv 폭이 화면보다 넓은 수면 평면 기준이라, 평면 두 끝을 매 프레임 투영해 `LIGHT_SWEEP`이 실제 화면 폭 대비가 되게 했습니다. 파문은 바꾸지 않았습니다.
- 검증: 바닥까지 스크롤 뒤 점의 최고 속도 43.7px/s(1440, 기준 90), 15.8px/s(390, 기준 35). 파문 영역 평균 차이 0.11/255, 모션 민감에서 x 불변.
- 브리프: docs/background-singapore-light-speed-brief.md
- 원문: `git show bf718be:docs/changelogs/changelog-september-23-2026-background-singapore-light-speed.md`

<a id="changelog-september-23-2026-background-chapter-map"></a>
## 2026-09-23 형상을 챕터에 맞춘다
- 범위: `lib/background/config.ts`, `lib/background/scene/BackgroundScene.ts`. 셰이더는 고치지 않음.
- 한 것: 구간 지도를 `SEOUL_WATERMARK.stages`(descendVh 1.2, revealVh 0.6, morph, singaporeBright 0.8, dissolve)로 모으고 `#join` 앵커를 읽게 했습니다.
  해가 내려가는 구간을 1.2화면으로 하고 서울이 설 자리를 만듦(`calmBright`, `portrait.naru*` 삭제). `#join` 앞에서 형상을 거두고 나루 점만 남김.
  빛 구멍은 나루 점 자체의 밝기 천장이라 셰이더를 고치지 않았고, 폰 `#record` 줄 대비는 별도 커밋으로 55% → 70%.
  사용자가 기준을 바꿈: 최고 휘도 120 → 95, 검증 3과 4는 점의 심만 가린 잉크 사용, 폰 서울 길이 23px 부족은 받아들임.
- 검증: 서울 온전 길이 2,083px(데스크톱)과 3,477px(폰), 서울 대비 싱가포르 1.13과 1.89. 본문 대비 최저 5.96(데스크톱)과 4.97(폰), em dash 0건.
- 브리프: docs/background-chapter-map-brief.md
- 원문: `git show bf718be:docs/changelogs/changelog-september-23-2026-background-chapter-map.md`

<a id="changelog-september-23-2026-seoul-presence"></a>
## 2026-09-23 서울의 존재감
- 범위: `lib/background/` 안만. 검증 5번 미달로 푸시하지 않은 로컬 커밋.
- 한 것: 윤곽 밝기 파도에 바닥을 두었습니다(`waveAmp` 0.3에서 0.12, `max(..., 0.8)`). 서울의 점을 싱가포르보다 굵게(`edgePxLandscapeSeoul` 5.0, `edgePxSeoul` 3.8) 하고 건너는 동안 `morph`로 보간합니다. 싱가포르 값은 그대로.
  브리프 이후 서울이 떠오르는 자리가 `#gains` 상단으로 옮겨져, 계측은 지금 서울이 서 있는 자리(데스크톱 y 4,600~5,180, 폰 y 5,910~6,512)에서 했습니다.
- 검증: 서울 대 싱가포르 밝기 데스크톱 모든 지점 120% 이상(전 86%), 싱가포르 변화 +1.1~+4.8%. 본문 대비 4.5:1은 미달(데스크톱 4.12, 폰 3.32), `#gains` 안내 문단에서 걸림.
- 브리프: docs/background-seoul-presence-brief.md
- 원문: `git show bf718be:docs/changelogs/changelog-september-23-2026-seoul-presence.md`

<a id="changelog-september-23-2026-background-one-clock"></a>
## 2026-09-23 배경의 시계를 하나로
- 범위: `lib/background/` 안만. DOM, 카피, 라우트, 수파베이스, `/2026-08`은 그대로. 커밋 넷.
- 한 것: 챕터 앵커(`#top` 하단, `#gains` 상단, `#naru`)에서 구간 진행도를 세어 `uStage1`, `uStage2`, `uMorph`로 넘김.
  해와 나루 점이 같은 `lightY`를 써서 한 불빛으로 이어지고, `uStage1 = 0.5`에서 수면을 지나 물 아래로 들어감.
  파문에서 `uScroll` 항을 빼고 사건(구간 1, 3)에 묶음. 서울은 수면이 가라앉은 뒤 `#gains` 상단부터 떠오름.
  점 개수, 크기, 형상 폭, 밝기와 `K` 관련 식, `crossing`, `field` 변형은 바꾸지 않음.
- 검증: 주황 0.35% 이상 미달(22% 지점 0.018% → 1.19%, 최저점은 구간 1 끝), 휘도 12.5 이상은 데스크톱 12.47로 0.03 미달, 폰 13.74.
  수면과 형상 안 겹침, 모션 민감 0px, `/2026-08` 잡음 안, 폰 60.2fps 통과.
- 브리프: docs/background-one-clock-brief.md
- 원문: `git show bf718be:docs/changelogs/changelog-september-23-2026-background-one-clock.md`

<a id="changelog-september-20-2026-hero-recede-removed"></a>
## 2026-09-20 히어로 스크롤 효과를 걷는다
- 범위: `components/home/NaruHome.tsx`의 히어로, `components/shared/useHeroRecede.ts`(삭제).
- 한 것: 사용자 요청("undo the animation that i tried to put at the top")으로 히어로 스크롤 효과를 전부 걷어 정지 레이아웃으로 돌렸다. 히어로 영역이 커밋 `c4de111`과 바이트 단위로 같다.
  9/19의 `useHeroSplit` 이식과 9/20의 `useHeroRecede` 둘 다 사용자가 원한 것이 아니었다는 기록을 `NaruHome.tsx` 주석에 남겼다. 세 번째 안을 만들기 전에 사용자에게 먼저 물을 것.
  챕터 리빌, `useHeroSplit.ts`와 `/2026-08`, 배경과 카피는 건드리지 않았다.
- 검증: 히어로 안 스크롤에 따른 불투명도와 transform 변화 0건, `framer-motion` import 0건, `npm run build` 통과.
- 브리프: docs/hero-recede-fix-brief.md
- 원문: `git show bf718be:docs/changelogs/changelog-september-20-2026-hero-recede-removed.md`

<a id="changelog-september-20-2026-hero-recede-fix"></a>
## 2026-09-20 히어로 효과의 대상을 제목 묶음으로 좁힌다
- 범위: `components/shared/useHeroRecede.ts`, `components/home/NaruHome.tsx`의 히어로 부분.
- 한 것: 값은 곡선대로 나왔지만 변하는 구간과 요소가 보이는 구간이 겹치지 않았습니다(데스크톱 제목은 페이드 시작 전에 사라짐, 폰은 처음 440px 동안 `transform: none`, `useHeroExit`의 ref 되먹임).
  효과 대상을 제목 묶음(아이브로, 제목, 날짜 줄, 설명)으로 좁혔고, CTA와 카운트다운은 효과 밖입니다. `useHeroExit`를 지워 폰과 데스크톱이 같은 `scrollY` 축을 씁니다. `isWide` 기본값을 `true`로.
  새 구간: 제목 묶음 데스크톱 0~300px(-120px), 폰 0~260px(-60px). 사진 단과 폰 두 번째 블록은 불투명도 0.2, 0.25에서 멈춥니다.
- 검증: 12개 항목 모두 통과. 스크롤 150px에서 데스크톱 제목 불투명도 0.577(58%가 헤더 아래), CTA 최솟값 1.000, `/2026-08` diff 0바이트.
- 브리프: docs/hero-recede-fix-brief.md
- 원문: `git show bf718be:docs/changelogs/changelog-september-20-2026-hero-recede-fix.md`

<a id="changelog-september-20-2026-schedule"></a>
## 2026-09-20 크로싱 서울 일정을 PDF대로
- 범위: `data/naru.ts`의 `december` 블록, `components/home/NaruHome.tsx` 프로그램 표, `components/shared/RouteMap.tsx` 주석 두 줄
- 한 것: 화면 일정이 하루씩 어긋나 있어 PDF대로 12/10을 Day 0(Context Open)으로, Day 4를 12/14로 바꾸고 세션 넷과 제출물 둘을 교체했습니다(`workshop`에서 `session`으로). PDF 05장은 사용자 지시로 제외했습니다.
  멘토링을 전 기간 상시에서 Day 1~3으로 좁히고, AI 활용 범위 셋과 운영 네 줄을 헤어라인 행으로 추가하고, `gaps[0]`을 PDF 문장으로 바꿨습니다.
  Day 0 행은 한 단 낮은 밝기(`/55`로 통일). 멘토링 기간, `tbd`의 "일정표" 제거 여부, Day 4 청중은 사용자가 정할 항목입니다.
- 검증: 날짜, 세션, 상자 수(36/29 유지), 영문, 글자 크기 통과. `#december` 높이는 실패(데스크톱 2,704에서 3,253), 대비는 활용 범위 ii만 4.37:1로 조건부 통과.
- 브리프: docs/schedule-2026-09-20-brief.md
- 원문: `git show bf718be:docs/changelogs/changelog-september-20-2026-schedule.md`

<a id="changelog-september-20-2026-presentation"></a>
## 2026-09-20 상자를 줄이고 목록으로 옮긴다
- 범위: `components/home/NaruHome.tsx`, `components/ui/typography.ts`, `app/globals.css`(`.naru-min12`), `data/naru.ts`의 감사 문단
- 한 것: 한 가지 상자 모양이 다섯 가지 일을 해 뜻을 나르지 못한다는 진단으로, 상자는 셋(누를 수 있는 것, 묶여야 하는 기록, 경고)만 쓰는 규칙을 `Card` 위에 적었습니다.
  `#gains` 카드 다섯을 목록 행으로, `#december` Day 카드 다섯을 노선도 아래 표 하나로, 8월 감사 문단을 여섯 줄 명단 + 닫는 한 줄로 바꾸고 매니페스토 상자를 뺐습니다. 문장은 새로 쓰거나 고치지 않았습니다.
  본문 글자 단계 열넷을 넷(12.24 / 13.5 / 15.75 / 18)으로, 모서리 반경을 둘(18, 알약)로 줄이고 `ROW_HEADING` 토큰을 추가했습니다.
  배경 입자가 걷힌 자리의 본문 대비를 4.5:1 아래로 떨어뜨려 브리프 지시대로 `main`에 푸시하지 않았고, 해결 방법(입자 밝기 낮추기 또는 옅은 판)은 사용자가 정합니다.
- 검증: 상자 수 55에서 36(데스크톱), 41에서 29(모바일)로 통과. `#december`와 문서 높이는 실패(데스크톱 9,315에서 9,715), 대비는 `#gains` 최저 1.84:1로 실패.
- 브리프: docs/presentation-review-brief.md
- 원문: `git show bf718be:docs/changelogs/changelog-september-20-2026-presentation.md`

<a id="2026-09-19-background-singapore-detail"></a>
## 2026-09-19 싱가포르 윤곽을 실제 해안선에서
- 범위: `scripts/build-shape-points.py`, `scripts/data/singapore-geoboundaries-2016.geojson`(신규), `lib/background/shapes/`(`singapore.ts`, `singapore-preview.png`, `README.md`), `lib/background/config.ts` 주석.
- 한 것: `#naru`의 싱가포르가 로고 SVG에서 구운 21각형이라 해안이 보이지 않았습니다. geoBoundaries gbOpen `SGP` ADM0(원자료 URA Master Plan 2014, CC BY 4.0)의 본섬 고리 하나(4,254 꼭짓점)로 바꿨습니다.
  부두와 방파제가 떨어진 점 줄로 보여 2,048px 래스터에서 형태학적 열기(반지름 24px, 약 590m)로 지우고 윤곽을 다시 땄습니다(브리프에 없던 단계).
  `singapore_ring()`을 지우고 서울과 같은 `geo_ring()` 경로로 합쳤고, `DP_TARGET`을 `DP_SEOUL`(60~90)과 `DP_SINGAPORE`(80~96)로 갈라 83 꼭짓점. 라벨은 넣지 않음(브리프 지시).
  초기화 비용이 데스크톱 5.6~5.7ms로 선(5ms)을 넘어 통과로 적지 않았고, 높이/너비는 0.455 → 0.511, 점은 1,987개.
- 검증: 8개 항목 통과. 서울 `seoul.ts` 바이트 단위 불변, 앞 500/1,100/1,400점 접두사 가장자리 비율 75.0%, 짝짓기 최소/최대 비 0.037(데스크톱)과 0.042(폰), `npm run build` 성공.
- 브리프: docs/background-ring-jump-and-singapore-detail-brief.md
- 원문: `git show bf718be:docs/changelogs/2026-09-19-background-singapore-detail.md`

<a id="2026-09-19-background-ring-phase"></a>
## 2026-09-19 파문 위상을 적분한다
- 범위: `lib/background/`의 `config.ts`, `scene/BackgroundScene.ts`, `water/WaterSurface.ts`, `shaders/water.ts`
- 한 것: 셰이더 위상이 `uTime × uFlow` 곱셈이라 스크롤 시작과 멈춤마다 파문이 한 프레임에 통째로 건너뛰었고, 오래 볼수록 점프가 커졌습니다(100초 뒤 약 16.7rad).
  위상을 `BackgroundScene`이 적분하고(`ringPhase += ringRate * dt * motionScale`), 셰이더는 `uRingPhase`를 받기만 합니다. `uFlow`는 이제 위상의 속도만 정합니다.
  새 상수는 `RING`(`baseRate 0.5`, `flowGain 1.6`, `maxRate 1.6`) 하나이고 기존 계수는 그대로입니다. shimmer 식(브리프 1.5)은 범위 밖이라 손대지 않았습니다.
- 검증: 폰 튕김 5회 601프레임 중 상한 초과 0건, 프레임당 최대 변화 옛 식 3.04rad 대 새 식 0.0079rad(385배). 데스크톱 정지 화면 5,184,000화소 중 차이 0.
  모션 민감에서 위상 정확히 0, 정지 0.50rad/s 대 스크롤 1.30rad/s, `npm run build` 통과(홈 262kB First Load JS 변동 없음).
- 브리프: docs/background-ring-jump-and-singapore-detail-brief.md
- 원문: `git show bf718be:docs/changelogs/2026-09-19-background-ring-phase.md`

<a id="2026-09-19-background-seoul-to-singapore"></a>
## 2026-09-19 #naru에서 서울이 싱가포르로 건넌다
- 범위: `lib/background/`의 `particles/ParticleField.ts`, `shaders/particles.vert.ts`, `scene/BackgroundScene.ts`, `config.ts`. DOM, 카피, `data/naru.ts`, 라우트, 수파베이스는 그대로.
- 한 것: `#naru` 반 화면 앞에서 서울 경계의 점들이 1.5화면에 걸쳐 싱가포르 본섬 윤곽으로 옮겨 서고, 위로 스크롤하면 같은 길로 돌아온다.
  짝짓기는 양쪽 점을 `phase`로 정렬하고 오프셋 16 x 방향 2 = 32개 후보 중 이동 거리 제곱합이 가장 작은 짝을 생성자에서 한 번 고른다(브리프의 32x2는 초기화 시간 상한을 넘어 16x2로 내림).
  셰이더는 `crossingMain`에 `uMorphOn`으로 가린 블록을 추가(water 변형만 1). `calm`(어두워짐)은 건너기가 끝난 뒤에 시작해 싱가포르가 온전한 밝기로 한 번 선다.
  새 값은 `singaporeW`와 `morph`(startVh 0.5, spanVh 1.5) 둘뿐이고 시간 없이 스크롤로만 움직인다. 라벨("SEOUL", "SINGAPORE")은 브리프 지시대로 넣지 않았다.
- 검증: `#naru` 앞 픽셀 차이 0개, 짝짓기 비 0.038(폰) / 0.037(데스크톱)(기준 0.5 이하), 초기화 4.6ms(데스크톱) / 4.1ms(폰).
  본문 대비 폰 `#naru` 도착 6.59:1, `#join` 8.69:1. 아카이브 `/2026-08`은 새 블록에 닿지 않음.
- 브리프: docs/background-seoul-to-singapore-brief.md
- 원문: `git show bf718be:docs/changelogs/2026-09-19-background-seoul-to-singapore.md`

<a id="2026-09-19-seoul-desktop"></a>
## 2026-09-19 데스크톱에서 서울 윤곽이 보이게
- 범위: `lib/background/config.ts`의 `SEOUL_WATERMARK` 세 값과 `BackgroundScene`의 호출 한 줄. 셰이더, DOM, 카피, 라우트는 그대로.
- 한 것: 사용자 요청("서울의 모습이 데스크톱 버전에서 너무 잘 안 보인다. 모바일은 잘 보인다.")에 따라 데스크톱 윤곽의 밀도를 올림.
  `landscapeW` 0.58 → 0.52, `points.desktop` 1,400 → 1,990(구워 둔 점 전부), 가로 화면 전용 점 크기 `edgePxLandscape: 4.2`, `innerPxLandscape: 2.0` 신설.
  세로 화면은 3.2 / 1.6 그대로. 폰은 지금이 좋다는 사용자 말에 따라 `N_POINTS`를 올려 다시 굽지 않음.
- 검증: `npm run build` 통과. 데스크톱 본문 대비 12.13:1 → 9.62:1(기준 4.5:1). 폰 전후 차이 17,005화소가 같은 코드 재로드 차이 16,830화소와 같아 불변.
- 원문: `git show bf718be:docs/changelogs/2026-09-19-seoul-desktop.md`

<a id="2026-09-19-background-ring-speed"></a>
## 2026-09-19 세로 화면 파문의 속도
- 범위: `lib/background/shaders/water.ts` 한 줄 (커밋 `9326bc4`). DOM, 카피, `data/naru.ts`, 라우트, 수파베이스는 그대로.
- 한 것: 파문 위상의 공간 주파수에도 `K`를 곱함(`rr * 9.0 * K`). 세로 패리티(`9d56472`)에서 `K`를 감쇠에만 붙여
  폰에서는 좁아진 범위를 예전 속도로 지나갔고, 이제 범위, 파장, 속도가 한 비율로 움직임.
  브리프 3.2(uFlow 게인, 파문 중심 이동)는 하지 않음. 실제 폰에서 보고 필요하면 각각 별도 커밋으로.
  0.7초 간격 캡처가 불가능해(스크린샷 왕복 40~58초) `prefers-reduced-motion`으로 `uTime`을 고정하고 마루 반경을 재는 방식으로 측정.
- 검증: 폰(390×844) 진행 속도 가로 12.0 → 5.6%/s, 한 주기 8.0 → 17.3초(계산). 데스크톱은 `K = 1.0`이라 코드상 항등, 실측 차이도 재로드 노이즈와 같음.
  폰 파문 지름 가로 42.5%, 본문 흰 글자 대비 6.79~8.40:1, 모션 민감 설정에서 프레임 차이 0.0.
- 브리프: docs/background-ring-speed-brief.md
- 원문: `git show bf718be:docs/changelogs/2026-09-19-background-ring-speed.md`

<a id="2026-09-19-background-portrait-parity"></a>
## 2026-09-19 세로 화면 배경을 가로 화면에 맞춘다
- 범위: `lib/background/` 안뿐(`shaders/water.ts`, `config.ts`, `renderer/PostFX.ts`). DOM, 카피, `data/naru.ts`, 라우트, 수파베이스는 그대로.
- 한 것: 세로 화면에서 `mask`가 `portrait`로 1이 되어 수면이 한 픽셀도 그려지지 않았고, 같은 `portrait`가 지평선 빛, 파문 링, 나루 점도 끄고 있었습니다.
  `mask`와 지평선 빛, 링, 점에서 `portrait`를 걷고, 가로 거리가 화면 높이 단위라 폰에서 링이 커 보이던 문제는 `K = mix(1.0, clamp(1.0/uAspect, 1.0, 2.4), portrait)`로 세 반지름을 보정해 풀었습니다.
  `config.ts`: `points.phone` 500 → 1100, 서울 `portrait` 폭 0.72 고정, 폰 티어 `bloom` 켬(`intensity` 0.85). `PostFX`는 블룸 기본 세기를 생성자 인자로(기본 0.6, 폰 0.42).
  가로 화면에서는 바뀐 표현이 모두 항등이라 데스크톱은 불변. 남은 것: 폰 수면이 DOM에 가려지는 문제는 범위 밖, 실기기 프레임과 링 크기는 미측정.
- 검증: 11개 항목 모두 통과. 폰 히어로 밴드 대비(배경만) 10.0배(기준 5배), 데스크톱 히어로 배경 픽셀차 0.0, 흰 글자 대비 최저 6.7:1.
  링 가로 지름 91% → 42%(분석), 모션 켠 폰 에뮬레이션 rAF 120.1fps.
- 브리프: docs/background-portrait-parity-brief.md
- 원문: `git show bf718be:docs/changelogs/2026-09-19-background-portrait-parity.md`

<a id="2026-09-19-new-url"></a>
## 2026-09-19 주소를 naru-crossing-seoul로 옮긴다
- 범위: Vercel 프로젝트 이름과 주소, 코드 안의 옛 주소 다섯 곳(`app/layout.tsx`의 `SITE_URL`, `app/sitemap.ts`, `app/robots.ts`, `components/Quiz.tsx`, `components/journey/Journey.tsx` 주석).
- 한 것: 사용자 요청(링크가 빌더톤이 아니라 나루, 크로싱 서울이어야 함)에 따라 프로젝트 이름을 `builderthon-for-korean-student`에서 `naru-crossing-seoul`로 바꿨습니다(이름은 물어서 정함).
  배포 보호(`ssoProtection: all_except_custom_domains`) 때문에 이름만 바꾸면 새 주소가 404였습니다. rename, 새 주소로 `vercel alias set`, 옛 별칭 `vercel alias rm` 순서로 새 주소를 프로덕션 별칭으로 만들었습니다. 배포 보호 설정은 건드리지 않았습니다.
  남은 것: 옛 주소로 공유된 링크는 전부 끊김, sitemap 재제출, 도메인을 사면 `SITE_URL` 한 줄만 교체, GitHub 레포 이름은 그대로.
- 검증: 새 주소의 `/`, `/2026-08`, `/quiz`, PDF, `/opengraph-image`, sitemap, robots 모두 200. 옛 주소는 404.
- 원문: `git show bf718be:docs/changelogs/2026-09-19-new-url.md`

<a id="2026-09-19-mobile-background"></a>
## 2026-09-19 폰 배경을 데스크톱과 같은 순서로
- 범위: `lib/background/scene/BackgroundScene.ts`의 `water` 분기 한 곳과 `lib/background/config.ts` 주석
- 한 것: 사용자 요청으로, 폰 첫 화면에 서울 윤곽이 먼저 보이던 것을 데스크톱처럼 나루 이미지(물과 해)로 시작해 내려가면서 서울 윤곽 점이 떠오르게 바꿨습니다.
  세로 화면이 9월 18일 모바일 브리프(6.1, 6.5) 결정 때문에 가로와 반대 순서였습니다. 이제 세로도 `revealVh` 곡선 하나를 쓰고, 히어로에서는 입자를 그리지 않습니다.
  세로에 남은 차이는 크기(110vw에서 90vw)와 밝기(0.8에서 0.5)뿐입니다. `SEOUL_WATERMARK.portrait.dissolveVh`는 더 이상 쓰이지 않지만 키는 남겼습니다.
- 검증: `tsc --noEmit`, `npm run build` 통과. 390x844 스크롤 0에서 서울 윤곽 없음, 데스크톱 1440x900은 전과 같음.
- 원문: `git show bf718be:docs/changelogs/2026-09-19-mobile-background.md`

<a id="2026-09-19-reveal-and-countdown"></a>
## 2026-09-19 스크롤 리빌, 카운트다운, 매니페스토 줄 정리
- 범위: 홈의 카운트다운 패널, 블록 리빌, 매니페스토 블록. 리빌 안전망 수정으로 `components/journey/Chapter.tsx`와 `app/globals.css`도 바뀜(`/2026-08`에도 적용).
- 한 것: 사용자 요청으로 매니페스토의 파일 정보 줄(`join.manifesto.meta`)을 데이터와 화면에서 뺐다.
  사용자 요청으로 카운트다운 행을 격자로 바꿔 상자를 채우고 숫자를 키웠다. 폰에서도 시간까지 보인다.
  사용자 요청으로 8월 페이지와 같은 스크롤 리빌을 홈에 달았다. 조사하니 CSS 안전망이 조건 없이 걸려 8월 페이지에서도 1.5초 뒤 전부 보이는 상태로 굳고 있었다.
  `<html>`에 `js-reveal-ready`를 달고 안전망을 `html:not(.js-reveal-ready)`로 좁혔으며, 1200ms 폴백은 화면 안의 경우로 좁혔다. 새 `components/shared/Reveal.tsx`로 `Chapter`와 같은 효과를 홈 블록 열일곱에 적용.
- 검증: `npm run build` 통과. 홈 리빌이 스크롤에 따라 y=0에서 1개, 4500에서 11개로 늘어남.
  카운트다운 1440px 614x166, 390px 336x139, 오버플로 없음.
- 원문: `git show bf718be:docs/changelogs/2026-09-19-reveal-and-countdown.md`

<a id="2026-09-19-share-card"></a>
## 2026-09-19 공유 카드를 다시 만든다
- 범위: `app/opengraph-image.tsx`, `app/layout.tsx`, `scripts/build-og-fonts.py`, `public/fonts/*.ttf`
- 한 것: 사용자가 외부 공유 시 카드 이미지와 설명이 예쁘지 않다고 요청. 카드를 CROSSING SEOUL 96px 한 가지 주인공으로, 로고는 가로 락업 `naru-name-rev.png`, 바탕 `#070B1F`에 방사형 빛 둘로 다시 그렸습니다.
  satori가 기본 서체 한 굵기로 그리던 것이 문제의 절반이라, Pretendard에서 굵기 450, 700, 900 정적 ttf(라틴과 문장부호만, 한 벌 28KB)를 뽑았습니다. 한글은 넣지 않았습니다.
  `OG_TITLE`, `OG_DESCRIPTION`을 새로 두어 이벤트와 날짜가 카드에 남게 했고, `SITE_DESCRIPTION`은 이벤트를 앞으로 당겼습니다. `/2026-08`, `/quiz` 카드는 범위 밖이라 그대로입니다.
- 검증: `npm run build` 통과, 1200x630 PNG 111,678 bytes, Black 900 임베드 확인, 카드 안 한글 없음, 설명 58자.
- 원문: `git show bf718be:docs/changelogs/2026-09-19-share-card.md`

<a id="2026-09-19-en-shorter"></a>
## 2026-09-19 영문을 짧게 줄인다
- 범위: `data/naru.ts`와 `data/dictionary.ts`의 `en:` 문자열만. 렌더 코드, 레이아웃, 한국어, 라우트, 수파베이스는 그대로.
- 한 것: 사용자 요청(영어 전환 시 문장이 너무 길다, 한국어는 그대로, humanizer 규칙을 쓸 것)에 따라 영문만 줄였고 `ko:` 줄은 diff에 0건입니다.
  humanizer 규칙 중 군더더기, 계사 회피, 부정 대구와 셋으로 늘리기, 과잉 수식, 과잉 헤징을 적용했습니다.
  두 집단을 모두 부르는 구절, FAQ의 약속(도입 비약속, 인턴십은 수상과 무관 등), 파트너 소개의 사실은 그대로 남겼습니다.
  남은 것: 8월 페이지 EN 300자 초과 문자열 열 몇 개(더 줄이려면 답의 범위 결정 필요), 곱슬 따옴표는 파일 전체 일관성 문제라 그대로.
- 검증: 홈 52문장, 영문 20,927 → 19,614자(-6.3%). `/2026-08` 50문장, 47,463 → 45,976자(-3.1%).
  `npm run build` 통과, 홈 EN 폰 문서 높이 11,577px(상한 15,000), 가로 오버플로 없음.
- 원문: `git show bf718be:docs/changelogs/2026-09-19-en-shorter.md`

<a id="2026-09-19-august-to-december"></a>
## 2026-09-19 8월 페이지의 마지막 화면을 12월과 잇는다
- 범위: `/2026-08`의 클로징(`#closing`) 블록과 그 카피(`data/dictionary.ts`의 `footer`, `components/journey/Journey.tsx`).
- 한 것: 사용자 요청("여기를 12월 이벤트 페이지랑 연결시켜줘")에 따라, "다음 무대"를 말하던 클로징에 12월로 가는 문을 달았습니다.
  `footer.nextStage` 문단과 `ctaDecember` 그라디언트 버튼(`/#december`, `next/link`, `naru_cta` 트래킹 `{ src: "august_closing", to: "december" }`)을 추가했습니다.
  이름, 날짜, 도시는 카피에 박지 않고 `lib/naruDates.ts`(`decemberEventLabel`, `formatDecemberRange`, `DECEMBER_CITY`)가 채웁니다. 파트너십 버튼은 그대로입니다.
  남은 것: 오픈채팅 버튼이 다시 열리면 둘 중 하나를 외곽선으로 내릴 것, 8월 내비에는 아직 12월 항목이 없음, `about.next`와 `about.cardLines[1]`에 박힌 날짜는 날짜가 바뀌면 같이 고쳐야 함.
- 검증: `npm run build` 통과, 버튼이 홈 `#december`에 도착. 390x844에서 버튼 255x63px, 가로 오버플로 없음.
- 원문: `git show bf718be:docs/changelogs/2026-09-19-august-to-december.md`

<a id="2026-09-19-why"></a>
## 2026-09-19 마지막 챕터가 묻는 질문을 왜로 바꾼다
- 범위: 홈 `#join` 챕터(`data/naru.ts`, `components/home/NaruHome.tsx`), `naruNav`의 라벨 하나, `public/naru/`의 매니페스토 PDF.
- 한 것: 마지막 질문이 절차("어떻게 함께하는가")였던 자리를 매니페스토 I장("우리가 본 것")이 가져갑니다. 새 제목 "왜 이 자리가 필요한가", `needs` 세 칸(싱가포르, 한국, 그 밖의 나라), `guards` 두 줄(하나만 넣으면 안 됨), `naruNav` 라벨 "함께" → "왜".
  사용자 두 번째 결정: "함께하는 길"을 내리고 그 공간을 "왜"에 씀. `milestones`, `closingStatement`, `closingBody` 추가. 규모 숫자는 `JOIN_STAT_CONFIRMED = false`로 그리지 않음.
  사용자 세 번째 결정: 카드 넷과 알럼 띠를 내리고(키는 남김) 매니페스토 PDF 다운로드 블록(`join.manifesto`, `naruLinks.manifesto`)을 둠.
  사라질 앵커로 가던 `#naru`의 문 셋은 `naruLinks.crew`, `organiser`, `sponsor` 메일로 바로 가게 고침(추적 이름 `naru_mail`).
  남은 것(사용자가 정할 것): 규모 숫자 출처, 칩 라벨을 "함께"로 되돌릴지.
- 검증: `npm run build` 세 커밋 각각 통과. 금지 표현, 비교 문장, em dash 0건, 폰 문서 높이 ko 11,646px / en 13,262px(상한 15,000), 끊어진 `href="#..."` 0건.
- 브리프: docs/why-brief.md
- 원문: `git show bf718be:docs/changelogs/2026-09-19-why.md`

<a id="changelog-september-19-2026-notdoing-removed"></a>
## 2026-09-19 "나루가 하지 않는 것" 블록을 내린다
- 범위: `components/home/NaruHome.tsx`의 `#naru` 챕터 끝 블록 하나
- 한 것: 사용자 결정으로 `#naru` 맨 아래 "나루가 하지 않는 것" 상자(회비와 가입 폼, 후원 계약과 법인격, 운영진 보수)를 화면에서 내렸습니다.
  렌더만 지웠고 `naru.how.notDoingLabel`, `naru.how.notDoing`과 금지어 예외 항목은 `data/naru.ts`에 그대로 두었습니다. 지운 자리에 이유와 키 위치를 적은 주석을 남겼습니다.
  이유: 챕터의 답은 세 층과 문 셋이 이미 하고, 마지막 인상 자리를 돈 이야기가 닫고 있었기 때문입니다.
- 검증: `tsc --noEmit` 통과, 홈 HTML에 해당 문구 0회, `#naru`가 세 층과 문 셋 링크로 닫힘(1728px).
- 원문: `git show bf718be:docs/changelogs/changelog-september-19-2026-notdoing-removed.md`

<a id="changelog-september-19-2026-day1-matching"></a>
## 2026-09-19 1일차: 트랙은 미리 고르고, 팀은 현장에서 맺는다
- 범위: `data/naru.ts` 문자열만(`december` 일정 블록, `gains.items[4]`)
- 한 것: 사용자 결정으로 1일차 전에 데이터를 공개해 트랙을 고르고 오고, 팀 매칭(한국 <> 싱가포르)은 1일차 현장에서 한 뒤 본딩을 거쳐 바로 Discovery로 넘어갑니다.
  `stages[0]`을 팀 본딩에서 트랙 선택(칩 `데이터 공개`)으로, `stages[1]`에 팀 매칭과 본딩 문장과 `팀 매칭` 칩을 넣었습니다. `scheduleLead`, `shape[0].label`, `flow[1]`도 같은 사실로 맞췄습니다.
  `gains` 05를 "같은 자리에 섭니다"에서 "한 팀이 됩니다"로 바꾸고, 이를 막던 주석을 경위로 교체했습니다. 1일차 시간표와 트랙 선택 방식은 미정입니다.
- 검증: `tsc --noEmit` 통과, 렌더된 HTML에 `팀 본딩`, `아이스브레이킹`, `사전 매칭` 0회. 데스크톱 1728px에서 데이 카드 다섯이 한 줄, 같은 높이.
- 원문: `git show bf718be:docs/changelogs/changelog-september-19-2026-day1-matching.md`

<a id="changelog-september-19-2026-gains"></a>
## 2026-09-19 얻는 것: 다섯 개에 설명을 단다
- 범위: 홈(`/`)의 `#gains` 챕터(`data/naru.ts`, `components/home/NaruHome.tsx`의 `<ol>`).
- 한 것: 9/17의 "제목만" 결정을 부분적으로 되돌려 `gains.items` 다섯에 "무엇인가"만 말하는 `body` 한 줄씩(한국어 36자 이하)을 달았습니다. 제목만으로 뜻이 안 통하는 항목과 빈 카드로 읽히던 02 때문입니다.
  `note`를 운영 방식은 확정되는 대로 채운다는 문장으로 교체했고, 05는 편성이 아니라 포지션만 말합니다. `evidence` 키는 그리지 않습니다(8월 숫자는 `#record` 몫).
  레일 `max-w-5xl` → `max-w-6xl`, `lg:min-h-[8.5rem]` 삭제, 폰은 1열 카드로 바꿨습니다.
- 검증: 1440에서 다섯 칸 한 줄(칸 폭 248px), 본문 최대 ko 2줄 en 3줄; 390 ko `#gains` 691 → 1,067px, 문서 10,375px; 가로 넘침 0.
- 브리프: docs/gains-detail-brief.md
- 원문: `git show bf718be:docs/changelogs/changelog-september-19-2026-gains.md`

<a id="changelog-september-18-2026-after"></a>
## 2026-09-18 after: 끝나면 할 일 세 줄
- 범위: `data/naru.ts`(`naru.after`), `NaruHome.tsx`의 `#december`
- 한 것: 8월 회차 뒤 멘토에게 먼저 연락한 팀이 한 팀뿐이었던 문제로, 처음에는 `#record`와 `#naru` 사이에 `#after` 챕터와 다리 링크 둘을 만들었습니다(커밋 `d66543f`까지).
  사용자 지시("이거를 위한 챕터를 만들지는 말고, 기존 포맷에 몇 줄 더 넣는 느낌")로 챕터, 헤더 항목, 다리 링크를 걷고, `#december`의 아쉬웠던 넷 아래에 같은 행 형식으로 "끝나면 할 일" 셋(`after.stepsLabel`, `after.steps`)만 남겼습니다. 데이터 키는 그대로 둡니다.
  `AFTER_CADENCE_CONFIRMED = false`로 7일과 한 달은 확정 전까지 화면에 없습니다. 멘토 명단 공유 동의, 공고 형식, 사례 게재 동의, cadence, "한 팀" 문구는 사용자가 정할 항목으로 남았습니다.
- 검증: `npx tsc --noEmit`, `npm run build` 통과, 콘솔 오류 0, em dash 0. 최종 문서 높이 390 약 11,700px, 1440 10,642px.
- 브리프: docs/after-brief.md
- 원문: `git show bf718be:docs/changelogs/changelog-september-18-2026-after.md`

<a id="changelog-september-18-2026-audit-fix"></a>
## 2026-09-18 감사 반영: 페르소나 8종 감사의 P0, P1, 선택 사항 전부
- 범위: 홈(`/`), 주로 `NaruHome.tsx`와 공유 컴포넌트. `/2026-08`은 그대로. 카피 키는 추가와 수정만. 커밋 여섯.
- 한 것: 사용자 결정으로 주황은 점으로만(그라데이션은 보라 → 자주 틴트), 민트와 초록은 General Mentoring 상자 하나, 나머지는 보라 `accent`.
  P0: "등록 준비 중" CTA(`aria-disabled`), 폰 헤더는 로고 줄만 접힘, Day 카드 폰 아코디언, 색 정리.
  P1: 나루 한 문장, 기업이 얻는 것, 8월 깔때기 스텝 차트(74 → 59 → 25 → 21 → 9), 참여 플로우 스트립 삭제, 아이브로와 보조 버튼 통일.
  a11y와 web-dev: 12px 하한과 44px 타깃, 헤더 정지 토글, 카운트다운 `role="group"`, 이미지 `sizes`, 메타(`canonical`, `languages`), `scroll-behavior` 제거.
  브리프와 다르게: `content-visibility: auto`는 스크롤 높이가 바뀌어 넣었다 뺌. 사람 탭은 한 줄 행(`RecordTabs compact`).
- 검증: 390 ko 합계 12,979 → 11,514(목표 10,800 미달, TODO: confirm), 1440 ko 10,349. `npx tsc --noEmit`, 빌드 통과, 콘솔 오류 0, 가로 넘침 0.
  `/2026-08` diff는 히어로 메탈 휴먼 이미지 영역뿐이고 글자와 레이아웃은 동일.
- 브리프: docs/audit-fix-brief.md
- 원문: `git show bf718be:docs/changelogs/changelog-september-18-2026-audit-fix.md`

<a id="changelog-september-18-2026-crossing-registration"></a>
## 2026-09-18 Supabase: 크로싱 서울 등록 표를 새로 만들고 12월에 연결
- 범위: `supabase/migrations/0004_crossing_seoul.sql`, `lib/register/shared.ts`, `lib/registrationWindow.ts`, `data/crossingForm.ts`, `app/api/crossing/register/route.ts`, `components/crossing/*`, 명단 스크립트. 8월 표, 뷰, 라우트, 모달, 마감은 변경 없음.
- 한 것: 12월 폼 질문이 다르고 미정이라(사용자 결정) 8월 표와 분리해 `crossing_registrations`, `crossing_members`, `crossing_participants` 뷰를 만들고, 미정 질문은 `answers jsonb`에 넣었습니다. RLS ON, 정책 0, 쓰기는 service_role만.
  8월 라우트의 공통 로직을 `lib/register/shared.ts`로 꺼냈고, `data/crossingForm.ts` 스키마가 곧 폼이며 서버와 클라이언트가 `validateField()`를 같이 씁니다. 추가 질문은 넣지 않았습니다.
  홈 CTA 셋을 등록 상태(not_open / open / closed)에 연결했고 등록 창은 열지 않았습니다(`CROSSING_WINDOW` 모두 `null`).
  등록 창 시각, 추가 질문, 동의 문구, 명단 docx 경로는 TODO: confirm.
- 검증: 8월 표 count 44 → 44, 74 → 74; 8월 라우트 응답 리팩토링 전후 동일; 12월 라우트 시나리오 (a)~(g) 기대 상태 코드대로 통과.
- 원문: `git show bf718be:docs/changelogs/changelog-september-18-2026-crossing-registration.md`

<a id="changelog-september-18-2026-merge-how-into-naru"></a>
## 2026-09-18 나루와 학생회와 기업을 한 챕터로
- 범위: 홈 `#naru`와 `#how`. 카피 키는 그대로.
- 한 것: 사용자 결정("나루와 학생회와 기업 내용은 하나의 챕터로 합쳐져야 함")에 따라 `#how`(세 층, 문 셋, 하지 않는 것)를 `#naru`의 마지막 블록으로.
  헤어라인 하나로 나누고 제목은 H2 → H3, 띠(BAND_TINT) 제거. 옛 링크를 위해 `id="how"` 유지. 헤더 앵커는 여섯.
- 검증: `npx tsc --noEmit`, `npm run build` 통과, 콘솔 오류 0. 섹션 순서가 헤더와 같음. 1440 ko 9,614 → 9,475, 390 ko 12,270 → 12,253.
- 원문: `git show bf718be:docs/changelogs/changelog-september-18-2026-merge-how-into-naru.md`

<a id="changelog-september-18-2026-mobile-fix"></a>
## 2026-09-18 모바일 수정: 폰에서 16.5화면을 줄인다
- 범위: 홈(`/`)의 lg 아래 렌더링과 세로 화면 배경. 데스크톱은 카운트다운 한 줄 외에 그대로. 커밋 넷.
- 한 것: `#december` 워크샵 상자와 참여 플로우는 lg부터만, Day 카드 본문 두 줄까지. 카운트다운은 한 줄, 서울 기준, 초 삭제.
  홈에만 스코프한 12px 글자 하한(`naru-min12`)과 44px 히트 영역. 8월 숫자 3 + 2, 언론 제목 두 줄, 헤더 현위치 칩 가운데 스크롤.
  세로 화면 배경(water + 서울 워터마크): 등불 가운데(x 50%), 수면, 수평선, 반사 기둥, 깊은 물의 링과 점 제거, 서울 워터마크 국면 조정.
  `RecordTabs` 사람 탭은 같은 날 사용자 지시(바로 8월로 보내라)를 따라 넣지 않음.
- 검증: 390 ko 합계 13,324 → 12,270(목표 10,800과 `#december` 2,600에는 미달, TODO: confirm). 12px 미만 40 → 1, 44px 미만 12 → 2.
  `/2026-08` diff 0(1440 스크롤 0.5 카메라 잡음 제외), fps 120/120, 콘솔 오류 0.
- 브리프: docs/mobile-fix-brief.md
- 원문: `git show bf718be:docs/changelogs/changelog-september-18-2026-mobile-fix.md`

<a id="changelog-september-18-2026-hero-countdown-tbd"></a>
## 2026-09-18 미정 목록 제거, 카운트다운 두 줄
- 범위: 홈 히어로 카운트다운, `#december`의 미정 상자, `lib/naruDates.ts`.
- 한 것: 사용자 결정으로 "아직 정해지지 않은 것" 상자를 화면에서 빼고, 일정 아래에 "새로 정해지는 것은 이 자리에 업데이트합니다." 한 줄을 두었습니다. 숫자 카드의 "목표." 접두어도 뺐습니다. `december.tbd`, `tbdLabel`, `tbdNote` 키는 그대로.
  사용자 결정으로 카운트다운을 서울 기준(12월 10일 0시 KST)과 싱가포르 기준(같은 날 0시 SGT) 두 줄로 바꾸고, 숫자 넷이 상자 폭을 채우게 했습니다(`DECEMBER_STARTS_AT_SG_MS` 추가).
- 검증: `npx tsc --noEmit`, `npm run build` 통과, 콘솔 오류 0. 페이지 길이 1440 ko 9,814에서 9,606, 390 ko 13,489에서 13,324.
- 원문: `git show bf718be:docs/changelogs/changelog-september-18-2026-hero-countdown-tbd.md`

<a id="changelog-september-18-2026-how-copy"></a>
## 2026-09-18 어떻게 일하는가: 두 문서에서 다시 짧게
- 범위: 홈 `#how` 챕터 카피만(`data/naru.ts`의 `how`). 마크업은 그대로.
- 한 것: 사용자 결정으로 `나루_Constitution.docx`와 `나루_Overview.pdf`를 다시 읽고 아주 간결하게 다시 쓰고, `humanizer.md` 기준으로 감사.
  리드를 세 문장으로 줄이고(hedge 문장 삭제), 다이어그램 가운데 문구를 "회차를 열고 책임진다"로, 하지 않는 것 셋을 회비와 가입 폼 한 줄, 후원 계약과 돈 한 줄(신규), 보수와 실비 정산 한 줄로 정리. 영문도 같이.
  감사에서 걸린 hedge 하나와 부정 병렬 하나를 고쳤고, 셋씩 묶인 구조는 문서 구조라 유지.
- 검증: `npx tsc --noEmit`, `npm run build` 통과, 콘솔 오류 0. 페이지 길이 1440 변화 없음, 390 ko -44px.
- 원문: `git show bf718be:docs/changelogs/changelog-september-18-2026-how-copy.md`

<a id="changelog-september-18-2026-bg-seoul-watermark"></a>
## 2026-09-18 배경: 스크롤하면 서울이 떠오른다
- 범위: 홈(`/`)의 `water` 배경 위 입자 층(`ParticleField`, `BackgroundScene`, `config.ts`의 `SEOUL_WATERMARK`, `shaders/water.ts`). `/2026-08`은 손대지 않음.
- 한 것: 사용자 결정에 따라 로고 심볼 작업은 커밋 없이 버리고, 9/15의 강과 등불 배경 위에 서울특별시 경계 입자 층을 얹었습니다. 히어로에는 없고, 스크롤하면 뷰포트 가운데에 떠올라 본문 뒤에 서 있으며 `#naru`부터 60% 밝기입니다.
  `ParticleField`에 `only: "seoul"` 인자와 `setShapeLook`, `BackgroundScene`에 `placeSeoul`을 추가했습니다. 점 수 데스크톱 1,400 / 폰 500.
  같은 날 2차(사용자): 서울 윤곽이 희미하다는 요청으로 점 밝기와 크기를 올리고, 빛의 원 시작 자리를 등불과 같은 위치로 옮겨 위로 움직이게 했습니다.
- 검증: `npx tsc --noEmit`, `npm run build` 통과; fps 1440 120 / 폰 에뮬 120; `/2026-08` 픽셀 diff 0.
- 원문: `git show bf718be:docs/changelogs/changelog-september-18-2026-bg-seoul-watermark.md`

<a id="changelog-september-17-2026-bg-water-return"></a>
## 2026-09-17 배경: 강과 등불로 복귀
- 범위: 홈(`/`)의 배경 변형 하나(`app/page.tsx`, `shaders/water.ts`). `/2026-08`은 그대로.
- 한 것: 사용자 지적("지금 background webgl이 8월 거랑 너무 똑같음")에 따라, 사용자가 세 안 중 9/15의 강과 등불(`water`)을 선택.
  `variant="crossing"` → `"water"`. crossing 변형과 형상 코드는 `lib/background`에 그대로 남김.
  가로 화면 등불 x를 0.30 → 0.50으로(두 단 사이 빈 틈). 세로 화면은 0.20 그대로.
- 검증: `npx tsc --noEmit`, `npm run build` 통과, 콘솔 오류 0. 히어로 H1 뒤 휘도 1440 0.0043 / 390 0.0034, fps 120 / 120.
- 원문: `git show bf718be:docs/changelogs/changelog-september-17-2026-bg-water-return.md`

<a id="changelog-september-17-2026-home-flow2"></a>
## 2026-09-17 홈 흐름 재배치: 히어로 아래에 사진 대신 답을 둔다
- 범위: 홈(`/`)의 챕터 순서와 `#december`, `#gains`, `#record`, 헤더 앵커, 배경 국면 앵커.
- 한 것: 히어로 바로 아래 사진이 연달아 나오고 시간이 오가던 문제로, 사용자 결정에 따라 무엇을 하는가(`#december`) → 무엇이 남는가(`#gains`, 신설) → 그게 진짜인가(`#record`) → 누가 만드는가 순서로 재배치했습니다.
  `#gains`는 번호 배지와 제목만 있는 카드 다섯(다섯 줄은 사용자가 정한 그대로, 영문은 초안). `#record`에서 사진 벽을 화면에서 빼고(키와 파일은 유지) 사람 탭을 넣었습니다.
  같은 날 2차(사용자): "너무 디테일하게 넣지는 말고" 요청으로 `#record`에서 사람 탭을 빼고 숫자, 언론 줄, 아카이브 버튼만 남겼습니다.
- 검증: `npx tsc --noEmit`, `npm run build` 통과; 2차 후 390 ko 13,533px, 1440 ko 9,814px; `/2026-08` 픽셀 diff 0.
- 브리프: docs/home-flow-brief.md
- 원문: `git show bf718be:docs/changelogs/changelog-september-17-2026-home-flow2.md`

<a id="changelog-september-17-2026-hero-photos"></a>
## 2026-09-17 히어로: 형상 대신 행사 사진 넷
- 범위: 홈(`/`) 히어로 오른쪽 단과 배경(`public/record/hero-*.webp`, `data/naru.ts`, `NaruHome.tsx`, `lib/background/config.ts`, `BackgroundScene.ts`). `/2026-08`은 그대로.
- 한 것: 사용자 요청(국가 이미지 대신 사람 많이 나온 행사 사진, 같은 사진 두 번 쓰지 말 것)에 따라 히어로 오른쪽 단을 8월 행사 사진 넷의 2×2 격자로(4:3, 1200×900 webp).
  배경의 싱가포르와 서울 형상, 등불, 반사 띠를 끔(`SHAPES.enabled = false`, 코드는 유지). 깊이 층 밝기 0.25 → 0.4. `ShapeLabels`와 무대 앵커 제거.
  2차로 사용자가 넷을 직접 고름. 그중 한 장이 8월 기록 벽의 `day1-start`와 겹쳐 그 자리를 다른 사진(`day1-full-hall.webp`)으로 교체하고 1차 파일 일부를 지움.
- 검증: `npx tsc --noEmit`, `npm run build` 통과, 콘솔 오류 0. 사진 넷 합 583 KB, 390 페이지 길이 +92px.
- 원문: `git show bf718be:docs/changelogs/changelog-september-17-2026-hero-photos.md`

<a id="changelog-september-17-2026-bg-fix"></a>
## 2026-09-17 배경 수정: 형상을 첫 화면 한가운데로, 배경을 다시 살아 있게
- 범위: 홈(`/`)의 배경 `variant="crossing"`과 히어로 레이아웃(`NaruHome.tsx`, 신설 `shapeLayout.ts`, `BackgroundScene`, `phases.ts`, 셰이더, `build-shape-points.py`). `/2026-08`의 `field` 경로는 그대로. 커밋 셋.
- 한 것: 형상이 첫 화면 밖(라벨 y 939)에 있고, 형상으로 안 읽히고, 가로선이 본문을 가르고, 깊이 층이 없던 문제를 고침.
  형상을 히어로 오른쪽 단의 무대(4:3)로 옮기고(싱가포르 왼쪽, 서울 오른쪽), 카운트다운은 왼쪽 단 CTA 아래 얇은 패널로.
  기울기 0°, 가장자리 띠 3%, 가장자리 점 75%, 윤곽을 따라 흐르는 빛. 블룸 임계값 0.7 → 0.9.
  반사 띠와 등불은 무대 폭 안에서만, 히어로가 나가면 사라짐. 8월 `field`와 같은 드리프트의 깊이 층을 되살리고, 국면은 px 기준(banks → gather → crossing → arrived → calm).
- 검증: 6개 해상도 전부 무대와 라벨이 첫 화면 안(PASS). 고정 수평선 없음(최악 비율 1.15). `/2026-08` 전후 픽셀 diff 전부 0px.
  fps 홈 1440 120/120, 폰 에뮬 120/120. `npx tsc --noEmit`, `npm run build` 통과, 콘솔 오류 0.
- 브리프: docs/background-fix-brief.md
- 원문: `git show bf718be:docs/changelogs/changelog-september-17-2026-bg-fix.md`

<a id="changelog-september-17-2026-openchat-off"></a>
## 2026-09-17 오픈채팅 문 닫기
- 범위: 사이트 전체의 오픈채팅 문(홈과 `/2026-08`), `data/dictionary.ts`, `components/shared/MobileChatBar.tsx`.
- 한 것: 사용자 요청("지금은 오픈톡방 막아줘. 나중에 추가해도 됨")으로 `links.openChat`을 빈 문자열로 두고 주소는 `links.openChatArchived`에 보관했습니다. 다시 열 때는 한 줄 되돌리면 됩니다.
  모든 자리가 `links.openChat &&`로 지켜져 있어 값 하나로 전부 숨고, 컴포넌트와 카피 키는 지우지 않았습니다.
  어색해지는 세 자리만 손봄: 히어로 "프로그램 보기"가 주 CTA 면을 받음, 참가자 카드에 "등록은 아직 열리지 않았습니다" 줄 표시, 폰 하단 바는 링크가 없으면 그리지 않음.
- 검증: `npx tsc --noEmit`, `npm run build` 통과; 렌더된 HTML에 `open.kakao.com` 홈 0건, `/2026-08` 0건.
- 원문: `git show bf718be:docs/changelogs/changelog-september-17-2026-openchat-off.md`

<a id="changelog-september-17-2026-hero-band-copy"></a>
## 2026-09-17 히어로 형상 띠, 언론 줄 정렬, 12월 카피
- 범위: 홈(`/`)만. `lib/background/utils/shapeLayout.ts`, `components/home/NaruHome.tsx`, `components/Background.tsx`, `lib/motionPreference.ts`, `components/shared/PressRows.tsx`, `data/naru.ts`.
- 한 것: 사용자가 지적한 문제(형상이 화면 아래에 붙음, 늦게 뜸, 언론 줄 정렬, "raw data" 표현, 12월 답)에 대응.
  형상을 히어로 그리드 아래 띠(`data-shape-anchor="band"`) 안에 두어 히어로가 한 덩어리로 가운데 잡힌다. 배경 idle 상한 1500 → 400ms, `naru:bg-ready` 이벤트 뒤에 라벨이 뜬다.
  언론 줄을 네 열 표 정렬로. "raw data"를 "데이터"로 바꾸고 12월 제목을 "데이터에서 증명까지, 닷새"로. 12월 답 02는 멘토링 디테일 사전 공유, 03은 모든 활동 대면.
- 검증: `npx tsc --noEmit`, `npm run build` 통과, 프로덕션 콘솔 오류 0(1440, 390). 네 화면 크기에서 형상이 카피와 패널 바로 아래.
- 원문: `git show bf718be:docs/changelogs/changelog-september-17-2026-hero-band-copy.md`

<a id="changelog-september-17-2026-record-press"></a>
## 2026-09-17 8월의 기록: 언론 줄
- 범위: `components/shared/PressRows.tsx`(신설), `data/naru.ts`, `components/home/NaruHome.tsx`. `/2026-08`은 그대로.
- 한 것: 사용자 요청으로 홈 `#record`에 8월 페이지의 기사 둘(경인일보, BZCF)과 싱가포르 현지 매체(CNA, The Straits Times)에 실렸다는 내용을 더했습니다.
  사진 벽 아래에 8월과 같은 인용 줄을 두고, CNA와 ST는 같은 보도자료라 한 줄에 링크 둘(`PressGroup`). 보도자료 면이라 "보도"나 "취재"라는 말은 쓰지 않음.
  홈 번들을 위해 기사 내용을 `data/naru.ts`에 옮겨 적음(한쪽을 고치면 다른 쪽도). Journey.tsx 원문은 그대로.
- 검증: `npx tsc --noEmit`, `npm run build` 통과, 콘솔 오류 0. 폰 ko 문서 13,026px로 476px 늘어 상한 12,500을 넘었으나 사용자 요청이라 유지.
- 원문: `git show bf718be:docs/changelogs/changelog-september-17-2026-record-press.md`

<a id="changelog-september-17-2026-bg-shapes"></a>
## 2026-09-17 배경: 기슭의 입자를 싱가포르와 서울 형상으로
- 범위: 홈(`/`) 배경 `variant="crossing"`의 기슭 입자 초기 분포(`lib/background/*`, `scripts/build-shape-points.py`, `components/home/NaruHome.tsx`, `data/naru.ts`). `/2026-08`은 손대지 않음.
- 한 것: 기슭 입자를 왼쪽은 싱가포르 본섬(로고 심볼의 섬 패스), 오른쪽은 서울특별시 경계(KOSTAT 2013, 64 꼭짓점으로 단순화) 윤곽 위에 점만으로 세웠습니다. 각 2,000점을 스크립트로 구워 `Float32Array`로 저장하고, 가장자리 먼저 순서를 씁니다.
  기울기 18°, 자리는 히어로 앵커(`data-shape-anchor`)를 DOM에서 재서 빈자리에 세웁니다. 브리프의 자리에서는 형상이 카피와 패널에 가려 보이지 않았기 때문입니다.
  사용자 결정(2026-09-17): 폰은 CTA 아래 150px 띠를 추가해 두 형상을 나란히 둡니다(ko 페이지가 상한 12,500을 50px 넘는 것은 감수). `SINGAPORE` / `SEOUL` DOM 라벨, gather 때 속 점부터 떠나도록 `aRank` 지연.
  하지 않은 것: 외곽선, 면, 주황 점, 지어낸 한강. TODO: KOSTAT 이용 약관 확인, 실기기 폰에서 윤곽 확인.
- 검증: 카피 뒤 휘도 1440 H1 0.0045에서 0.0043, fps 전 조건 120. `/2026-08` 회귀 스크롤 0에서 0px 차이, `tsc`와 build 통과.
- 브리프: docs/background-shapes-brief.md
- 원문: `git show bf718be:docs/changelogs/changelog-september-17-2026-bg-shapes.md`

<a id="changelog-september-17-2026-bg-crossing"></a>
## 2026-09-17 배경: 건너는 점들
- 범위: 나루 홈(`/`)의 배경(`lib/background/` 셰이더, `ParticleField`, 신설 `Lantern.ts`, `PostFX`, `phases.ts`, `BackgroundScene`, `app/page.tsx`). `/2026-08`의 입자 필드는 그대로.
- 한 것: 8월 엔진을 그대로 쓰고 이야기만 바꿈. 양쪽 기슭의 점들이 스크롤에 따라 어두운 강을 건너 등불 하나(나루 점) 쪽으로 모임.
  국면은 banks → gather → crossing → arrived이고 경계는 `#record`, `#december`, `#naru` 앵커에서 읽음. 화이트아웃 없음, 주황은 등불과 반사 기둥뿐.
  셰이더는 파일 복제 없이 uniform(`uMode`)으로 분기해 `field`, `water` 경로 원문은 불변.
- 검증: `/2026-08` 회귀 diff 0px(1440 스크롤 0.5는 잡음 6,078px보다 작은 2,591px). 정지 토글 픽셀 diff 0, 콘솔 오류 0, 카피 뒤 휘도 전부 8월보다 낮음.
  fps 홈 1440 120/113, 폰 근사 120. 실기기 fps는 TODO: confirm.
- 브리프: docs/background-crossing-field-brief.md
- 원문: `git show bf718be:docs/changelogs/changelog-september-17-2026-bg-crossing.md`

<a id="changelog-september-17-2026-august-grammar"></a>
## 2026-09-17 나루 홈을 8월 사이트의 디자인 문법으로
- 범위: 홈(`/`)의 보이는 문법, 공용 컴포넌트 추출(`components/ui/`, `components/shared/`), `Journey.tsx`
- 한 것: 홈과 `/2026-08`이 다른 문법이라, 카피 키와 챕터 순서는 그대로 두고 8월의 두 단 히어로, 그라데이션 제목, 발광 챕터 제목, 번호 배지 카드, 노선도, 플로우 스트립, 그라데이션 필 CTA 등을 홈에 입혔습니다.
  `Glass`, `FlowStrip`, `Band`, `MobileChatBar`, `HeroPartnerStrip`, `useHeroSplit`, `Chip`, `Button`을 `Journey.tsx`에서 꺼냈고(4,993줄에서 4,353줄), `Halo`와 prop 기반 `RouteMap`은 새로 썼습니다.
  원칙: 팔레트는 나루 토큰(`GRADIENT_TEXT`), 주황은 면이 아니라 점, 길이 상한 준수, 8월 페이지는 픽셀 하나도 달라지면 버그.
- 검증: 8월 페이지 픽셀 diff 1440 ko/en 0px, 390 ko 154px(지연 로딩 사진 디코드 차이). 홈 390 ko 12,391px, 1440 ko 9,289px로 상한 안.
- 브리프: docs/august-design-language-brief.md
- 원문: `git show bf718be:docs/changelogs/changelog-september-17-2026-august-grammar.md`

<a id="changelog-september-17-2026-flow"></a>
## 2026-09-17 홈의 흐름: 12월을 앞으로, 행동을 제목 아래로
- 범위: `/` 홈만(`data/naru.ts`, `components/home/NaruHome.tsx`, `components/ui/OpenChatLink.tsx`, `components/ui/typography.ts`, `lib/naruDates.ts` 등).
- 한 것: 다섯 평가가 12월이 너무 멀고 도착해도 할 일이 없다고 짚어, 챕터 순서를 `top, record, december, how, join, why`로 바꾸고 오픈채팅을 `#december` 제목 바로 아래 흰 면 버튼(`variant="primary"`)으로 올렸습니다. `#how` 층 카드에서 `#join` 카드로 가는 링크, 메일 제목 통일, 9/16에 떨어진 문장 셋(해외 한인 유학생, 스크리닝 없음 등) 복원.
  같은 날 추가: 사용자 요청으로 `#why` 코어 둘을 전면 폭 판으로 다시 그렸고(`STATEMENT` 토큰), 두 번 축소했으며, 사진 벽의 중복 사진 하나를 교체했습니다.
  사용자 요청으로 이벤트와 그룹을 나눴습니다: 히어로는 크로싱 서울, `#december`는 기획 PDF 기반 프로그램(초안 표시), 아래에 `#naru` 그룹 챕터. 날짜 12/10~12/14 확정.
  이후 사용자 요청으로 걷어내기(폰 16,045px에서 12,010px), 숫자 여섯 칸 제거, 일정 카드 줄 맞춤과 General Mentoring 가로 상자.
  TODO: 주관 학생회 확정, 12/14에 무엇이 있는지 확정.
- 검증: 390x844에서 `#december` 제목 5.9화면에서 3.4화면, 첫 행동 8.6화면에서 3.9화면. `npx tsc --noEmit` 통과, 가로 넘침 0.
- 원문: `git show bf718be:docs/changelogs/changelog-september-17-2026-flow.md`

<a id="changelog-september-16-2026-essence-only"></a>
## 2026-09-16 코어만 남깁니다
- 범위: 나루 홈(`/`, `NaruHome.tsx`, `data/naru.ts`), `/2026-08`, `/quiz` CTA, `shaders/water.ts`, `app/globals.css`, `typography.ts`, `/12` 클래스가 있던 컴포넌트 7개.
- 한 것: 챕터마다 그 챕터만 말할 수 있는 것 하나만 남기고 나머지는 화면에서 내림(키는 하나도 지우지 않음). `/2026-08`의 `#wrap` 챕터 제거.
  8월 챕터는 숫자 다섯 + 사진 열두 장(`PhotoWall`), 12월 챕터는 기획 초안의 엑기스(`december.shape` 여섯, `stages` 다섯, 날짜 없이, `draftNote` 유지).
  `#why`를 페이지 맨 아래 결론으로 옮기고 다시 길게 씀(코어 둘 + 지키는 것, `why.exec`, `why.measure`). 이음매 306px, 서명 헤어라인, 나루 점, 제목 위계(`LABEL_HEADING`).
  시상식 사진 둘 제거, 사진 원본을 4:3으로 다시 잘라 균일 grid로(장수는 6의 배수). 배경에 깊은 물 층과 스크롤 속도 `uFlow` 추가.
  Tailwind v3에서 무효인 `/12` 불투명도 37곳을 `/[0.12]`로, `bg-clip-text` 단색 폴백, 세로 간격을 한 스케일로(이음매 216/270/306), 카드의 번호 줄 제거.
- 검증: `#why` DOM 순서 = nav 순서, 나루 점 2개, 서명 헤어라인 1개, 가로 넘침 0. 사진 칸 전부 372x279. 컴파일된 CSS에서 `/[0.12]` 여섯 선택자 규칙 생성 확인.
- 원문: `git show bf718be:docs/changelogs/changelog-september-16-2026-essence-only.md`

<a id="changelog-september-15-2026-crossing-seoul-positioning"></a>
## 2026-09-15 크로싱 서울 포지션 반영
- 범위: 주로 카피(`data/naru.ts`, `data/dictionary.ts`), `components/home/NaruHome.tsx`의 `#december` 제목 구조, `lib/naruDates.ts` 포맷 함수 둘, `app/layout.tsx`, `app/opengraph-image.tsx`.
- 한 것: "무대를 한국으로 옮깁니다"가 서울 개최를 한국 이벤트로 읽히게 해서, 결정한 포지션 "크로싱 서울 = 한인 학생 빌더가 국경과 상관없이 만나는 자리"로 카피를 바꿨습니다. 영문에서 `Korean`은 빼지 않았습니다.
  `#december` H2를 날짜에서 포지션 문장으로 바꾸고, 이름은 아이브로로, 날짜는 한 줄(`formatDecemberDateLine` 신설)로 내렸습니다.
  브리프와 다르게 한 것 셋: 375px 접힘 때문에 아이브로에서 날짜를 뺌, 영문 H2 축약, 히어로 서브에서 서울 반복 제거.
  히어로 아이브로, 귀국 시기 줄, 한국 쪽 주관 주체, 12월 종료일 등은 TODO: confirm으로 남겼습니다.
- 검증: `npx tsc --noEmit`, `npm run build` 통과; 금지 표현("글로벌" 등, "무대를 한국으로") 0건; 375px 가로 오버플로 0.
- 원문: `git show bf718be:docs/changelogs/changelog-september-15-2026-crossing-seoul-positioning.md`

<a id="changelog-september-15-2026-naru-launch"></a>
## 2026-09-15 나루 런칭 홈, 제로백 빌더톤을 /2026-08 기록으로
- 범위: 새 홈(`/`)과 `data/naru.ts`, `lib/naruDates.ts`, 8월 페이지 이동(`app/2026-08/page.tsx`), 팔레트, 로고 자산(`public/naru/`), 메타데이터. Supabase, `/api/register`, `/api/vote`는 그대로.
- 한 것: 홈은 이벤트가 아니라 그룹(나루)을 말한다: 히어로, `#why`, `#record`, `#how`, `#december`, `#join` 챕터와 푸터. 홈에는 등록이 없고 CTA는 셋(이벤트 알아보기, 소식 받기, 문의)이며 `useRegisterOptional()`을 추가.
  12월은 제로백 빌더톤의 2회차가 아니라는 구분을 지키려고 이름과 날짜를 `lib/naruDates.ts` 한 곳에서만 읽고, `december` 블록의 예외는 부정문 `december.notSequel` 한 줄뿐. 같은 날 이름은 크로싱 서울(`DECEMBER_EVENT_NAME`)로 정했다.
  8월 페이지는 지우지 않고 `/2026-08`로 옮겨 `serverNow`를 고정하고 배너를 달았다. 크레딧은 고치지 않았고, 사용자 지시로 아카이브의 "강남 쇼케이스" 서술을 크로싱 서울로 고치고 무산된 글로벌 해커톤 칸을 삭제했다.
  팔레트를 로고 가이드 v1(남색 `#12246B` 등)로 옮기고, 한글이 든 로고는 PNG로 둔다. `#record`에 8월 정본을 직접 읽는 탭 셋(`RecordTabs.tsx`), 사진 셋, 링크드인을 달았다.
  디자인 리뷰 뒤 배경을 나루터 수면 셰이더(`water.ts`)로 바꾸고 `motionScale`을 0으로, 탭 키보드 패턴, 배경 정지 토글(`MotionToggle.tsx`), 홈 헤더에서 퀴즈 제거.
- 검증: `npx tsc --noEmit`, `npm run build` 통과(12페이지 정적). 375px와 1440px에서 가로 오버플로 없음, 콘솔 오류 없음.
- 원문: `git show bf718be:docs/changelogs/changelog-september-15-2026-naru-launch.md`

<a id="changelog-august-12-2026-day2-team-building"></a>
## 2026-08-12 Day 2 팀 빌딩 타임 신설, 맞춤 세션 추천 제거
- 범위: `data/schedule.ts`, `data/dictionary.ts`, `components/journey/Journey.tsx`(칩 삭제), `app/quiz/page.tsx`(메타데이터), `components/Quiz.tsx`(주석).
- 한 것: 8/12 주최 결정에 따라 Day 1 현장 매칭 팀을 위한 Day 2 크래시코스 후 팀 빌딩 카드 `d2-team-building`(주관 학생회, 현장, 장소 추후 안내)을 추가했습니다. 시각은 추론하지 않고 비워 뒀고, 식사 언급 없음과 운영 세부 비노출을 주석으로 못박았습니다.
  Day 2 데이 칩을 `mixed`가 아닌 `online-default`로 바꾸고, Day 2 요약, Day 1 `runOfShow` 매칭 줄, FAQ 두 곳에 팀 빌딩 연결 문장을 넣었습니다. FAQ는 새 사실만 반영하고 앞서 정리된 톤과 용어는 유지했습니다(지시와 다르게 한 부분).
  RegisterModal 안내는 자리가 억지스러워 넣지 않았습니다.
  사용자 요청으로 존재하지 않는 기능을 파는 "유형 테스트 결과로 맞춤 세션 추천받기" 칩(`dict.programQuizChip`)을 삭제하고, 퀴즈 메타데이터 세 곳을 결과가 실제로 담는 내용으로 고쳤습니다.
- 검증: `npx tsc --noEmit` 통과; 브라우저 KR, EN에서 Day 2 모달과 FAQ 확인; 홈 HTML에서 `맞춤 세션` 0건.
- 원문: `git show bf718be:docs/changelogs/changelog-august-12-2026-day2-team-building.md`

<a id="changelog-august-11-2026-faq-trim-question-tone"></a>
## 2026-08-11 FAQ 16 → 14문항: 중복 정리 + 질문 톤 소프트닝
- 범위: `data/dictionary.ts`의 `faq.items`와 바뀐 질문을 인용하던 주석 세 곳. 렌더 코드와 `data/schedule.ts`는 그대로.
- 한 것: 같은 사실(완성도 무관, 수료증 조건, 인턴십 조건)이 여러 항목에 흩어져 있고, 같은 질문이 두 번 있었고, 질문 워딩이 사이트 스스로를 의심하게 만들었습니다.
  "제가 여기서 얻는 게 뭔가요?"를 삭제하고 그 내용을 "8일이 끝나면…" 답에 흡수, 문과와 해커톤 항목을 "코딩도 발표도 자신이 없어요. 괜찮을까요?"로 병합.
  상금 답의 수료증 문장 삭제, 결과물 답의 인턴십 조건을 인턴십 답변으로 넘김. 라이브 시연 문장은 오해 방지를 위해 유지. 질문 여섯 개를 소프트닝(EN 포함), 크래시코스 답에 `네,` 추가.
  `DECIDED 2026-08-04`(발표는 전부 한국어) 등 결정 주석은 살아남는 항목으로 이관하고 `REMOVED / MERGED / ABSORBED 2026-08-11`을 남김.
- 검증: `npx tsc --noEmit` 통과, `faq.items.length === 14`, 열네 항목 첫 문장 모두 직답, 브라우저 KR과 EN 렌더 확인.
- 원문: `git show bf718be:docs/changelogs/changelog-august-11-2026-faq-trim-question-tone.md`

<a id="changelog-august-10-2026-day8-track-slots-unify"></a>
## 2026-08-10 Day 8 트랙 발표 두 슬롯: 문안 통일 + 시간 구성 헤지
- 범위: `data/schedule.ts` (새 상수 `D8_TRACK_PITCH_NOTE`, `days[7].runOfShow`의 트랙 발표 두 줄, `d8-judging`의 `summary`와 `description`)
- 한 것: 트랙만 다른 같은 세션인 두 슬롯이 각각 반쪽 안내(발표 포맷만, 자유시간만)를 달고 있어 공통 상수 `D8_TRACK_PITCH_NOTE` 하나를 참조하게 했습니다.
  8분 안의 발표와 Q&A 배분이 미확정인데 확정처럼 세 곳에 적혀 있어, 확정 전에는 총량(팀당 8분, 잠정)과 "구성은 확정되는 대로 안내"만 노출하기로 했습니다.
  전체 grep으로 Day 8 배분 표기가 남은 곳이 없음을 확인했고, 상수와 `d8-judging` 주석에 확정 전 세부 배분을 되살리지 말라는 경고를 남겼습니다.
- 검증: `npm run build` 통과(10/10 static pages), `Bilingual` 타입으로 ko/en 쌍 강제.
- 원문: `git show bf718be:docs/changelogs/changelog-august-10-2026-day8-track-slots-unify.md`

<a id="changelog-august-10-2026-daycard-routemap-sync"></a>
## 2026-08-10 노선도와 데이 카드 의미 체계 동기화
- 범위: `data/schedule.ts`(`DayEmphasis`, `dayEmphasis()`, `MENTORING_DAYS`, `mentoringOpenOn()`, `MENTORING_DAY_RANGE`), `components/journey/Journey.tsx`, `data/dictionary.ts`
- 한 것: 노선도는 3층(필참, 놓치면 아까운, 선택)과 Day 3~7 멘토링 마커를 쓰는데 데이 카드는 2층에 멘토링 표시가 없어, 판정을 `schedule.ts`로 올리고 두 표면이 여기서만 도출하게 했습니다. 새 데이터 필드는 만들지 않았습니다.
  카드 배지를 3층으로(◉ 놓치면 아까운, violet), 스포트라이트 날 모달에 "선택 참여" 칩을 짝으로 붙이고, 멘토링 날 카드에 `whyStop` 아래 "1:1 멘토링" 칩을 자기 줄로 달았습니다.
  `SpotlightGlyph`, `MentoringDot` 컴포넌트로 기호를 공용화했고, 스포트라이트에 배지를 달지 않던 기존 결정은 되돌리고 주석에 그 거래를 기록했습니다. 카드 테두리는 3단계로 나누지 않았습니다.
- 검증: 8일 대조표 불일치 0건(8/8), 멘토링 구간 `{from: 3, to: 7}`. `npm run build` 통과, 375px `scrollWidth` 375로 가로 넘침 없음.
- 원문: `git show bf718be:docs/changelogs/changelog-august-10-2026-daycard-routemap-sync.md`

<a id="changelog-august-10-2026-routemap-mentoring-dots"></a>
## 2026-08-10 노선도 멘토링: 연속 선에서 점 마커로
- 범위: `components/journey/Journey.tsx`(`RouteMap`, `MENTORING_DAYS`, `MENTORING_DAY_RANGE`), `data/dictionary.ts`(`program.route.mentoringBand` 변경, `mentoringBandAria` 삭제)
- 한 것: 모바일에서 노선도가 두 행으로 접히며 Day 3~7 멘토링 연속 선이 두 토막 나고 첫 줄 토막에 라벨이 없어, 멘토링 날 노드마다 에메랄드 점을 찍는 방식으로 바꿨습니다. 데스크톱도 같은 점으로 통일했습니다.
  날짜는 `category === "mentoring"` 이벤트에서 파생하고, 필 문구는 "1:1 멘토링" 뒤에 Day {from}~{to} 구간을 붙여 구간을 말로 채웁니다. 문두 점은 문자가 아닌 렌더된 점입니다.
  같은 날 후속으로 모바일 행 사이 화살표를 지우고(여백 36px 유지), 점을 5px에서 7px로 키웠습니다(더 키우지 말라는 주석). 모바일 노선도는 32px 짧아졌습니다.
- 검증: `npm run build` 통과(`/` 61.4 kB, 249 kB First Load). 375px KO/EN에서 점 한 줄 정렬, `scrollWidth` 375로 가로 넘침 없음.
- 원문: `git show bf718be:docs/changelogs/changelog-august-10-2026-routemap-mentoring-dots.md`

<a id="changelog-august-10-2026-hero-cta-register"></a>
## 2026-08-10 히어로 주 CTA를 등록으로
- 범위: `components/journey/Journey.tsx`, 모듈 상수 `HERO_PRIMARY`와 히어로 CTA 클러스터.
- 한 것: 모바일 첫 화면에 등록 진입점이 없어서(스티키 바는 히어로에서 숨고 네비 등록은 lg 미만 미렌더), 등록 마감 국면에 맞춰 히어로 주 CTA를 "등록하기"(`openRegister()`)로 바꿨습니다.
  `HERO_PRIMARY: "journey" | "register"` 상수 한 줄로 되돌릴 수 있게 journey 마크업을 분기로 남겼습니다.
  등록한 방문자에게는 `dict.register.navRegistered` 라벨; 트래킹 `track("register_click", { src: "hero" })` 신설. register 모드에서 파트너십 문의 버튼은 히어로에서 뺐습니다(클로징과 푸터에 그대로 있음).
  스티키 바, 네비, 등록 훅 카드는 그대로 둡니다(훅 카드는 솔로 매칭 프리셋이라 의도된 중복).
- 검증: `npx tsc --noEmit`, `npm run build` 10/10 통과; 375px ko 2행, en 3행으로 깔끔히 랩.
- 원문: `git show bf718be:docs/changelogs/changelog-august-10-2026-hero-cta-register.md`

<a id="changelog-august-10-2026-mobile-polish"></a>
## 2026-08-10 모바일 폴리시 4건
- 범위: `components/journey/Journey.tsx`(`RouteMap`, `LogoTile`, 주관 티어 그리드), `data/dictionary.ts`(`program.route.principle`, `route.destination` 제거), FAQ 어워드 답변은 검증만.
- 한 것: 노선도에서 Day 6만 흐려 보인 것은 세션 유무 로직이 아니라 이웃 대비 탓이었습니다. 규칙은 층 하나뿐이라 선택일 네 칸을 함께 `text-white/60`에서 `/75`로 올렸습니다.
  `route.destination` 줄을 지우고 `principle` 첫 문장에 흡수했습니다(지시어가 가리키던 무대 이름이 바로 그 줄이었음). 범례 3종은 유지했습니다.
  주관 티어의 SMU 라이온 실루엣은 같은 면적에서 가장 커 보여 `area: 1450`(34px에서 29px)으로 보정하고 `area` 주석을 고쳤습니다.
  FAQ 어워드 답변은 새 구조(순위 없음, 테마형 4부문, 현금 또는 바우처)와 이미 일치해 변경하지 않았습니다.
  되돌리지 말 것: Day 6만 굵게 하기, 행선지를 독립 줄로 빼기, 스포트라이트 날에 배지 달기, `area` 값을 다른 실루엣에 복사하기.
- 검증: `npx tsc --noEmit`, `npm run build`(10/10) 통과. 노선도 노드 색 `#fff` 4 / `rgba(255,255,255,.75)` 4, 주관 티어 높이 SMU 29px, NUS 44px, NTU 44px.
- 원문: `git show bf718be:docs/changelogs/changelog-august-10-2026-mobile-polish.md`

<a id="changelog-august-04-2026-modal-scroll-lock"></a>
## 2026-08-04 모바일 모달 스크롤 격리
- 범위: `lib/useBodyScrollLock.ts`(신규), `EventModal`, `PartnerModal`, `RegisterModal`, `Journey.tsx`(`DayModal`과 스크롤 소비자), `JourneyNav.tsx`, `lib/useScrollDirection.ts`, `BackgroundScene.ts`.
- 한 것: 모달이 열린 채 배경이 스크롤되고 내부 스크롤이 배경으로 체이닝되던 버그. `html { overflow-x: clip }` 때문에 `body` overflow hidden이 전파되지 않았고 iOS는 이를 무시합니다.
  네 곳의 잠금을 `useBodyScrollLock` 하나로 대체: `body`를 `position: fixed; top: -scrollY`로 빼고, 풀 때 `behavior: "instant"`로 복원. 중첩 모달을 위한 깊이 카운터를 둠.
  보조로 내부 컨테이너에 `overscroll-contain`, 백드롭에 `touch-none`. 락 중 `window.scrollY`가 0이 되므로 소비자들이 `isScrollLocked()`로 마지막 값을 유지.
  데스크톱 스크롤바 6px 밀림은 의도적으로 보정하지 않았습니다(padding, `overflow-y: scroll`, `scrollbar-gutter` 모두 부작용이나 무효).
- 검증: 네 모달 모두 열린 상태에서 강제 스크롤해도 `window.scrollY` 불변, 닫으면 정확히 복귀(중첩 시 9969px 복원). ESC, 포커스 복귀 유지, `npm run build` 통과.
- 원문: `git show bf718be:docs/changelogs/changelog-august-04-2026-modal-scroll-lock.md`

<a id="changelog-august-04-2026-why-stop-lines"></a>
## 2026-08-04 선택일 카드에 '올 이유' 한 줄
- 범위: `data/schedule.ts` (`DayMeta.whyStop`, `DayMeta.stopLabel`), `components/journey/Journey.tsx` (`DayCard`, `RouteMap`), `data/dictionary.ts` (`program.route.optionalValue`).
- 한 것: "하나하나 내려설 이유가 있도록 설계했다"는 주장을 카드가 증명하도록 Day 2~7에 `whyStop` 한 줄(에메랄드, `→` 프리픽스)을 추가했습니다. 필참일(Day 1, 8)에는 `!day.mandatory` 가드로 렌더하지 않습니다.
  Day 5는 프로그램이 아니라 만남을 이유로, Day 6은 가짜 이유 없이 "정해진 것 없음"으로 썼습니다.
  `stopLabel` override로 노선도의 Day 3, 4 키워드를 "자율 빌드"에서 "1:1 멘토링"으로 바꿨습니다.
  `optionalValue`의 가운데 부연을 덜어내고, Day 6 요약의 중복 문장을 정리했습니다.
- 검증: `npx tsc --noEmit` 0, `npm run build` 10/10; 375px에서 KR 1줄(Day 7만 2줄), EN 2줄; 카드 최대 높이 296px, 가로 오버플로 0.
- 원문: `git show bf718be:docs/changelogs/changelog-august-04-2026-why-stop-lines.md`

<a id="changelog-august-04-2026-prompt-question-clarify"></a>
## 2026-08-04 심사위원 질문 인용 풀어쓰기
- 범위: `data/dictionary.ts`, 심사위원 질문 인용부 2곳(KO/EN).
- 한 것: 축약형 "프롬프트 한 줄로 한 것과 뭐가 다르죠?"가 심사위원조차 의미를 되물을 만큼 오독되어, 비교 대상(담당자의 대안)을 넣어 "담당자가 그냥 범용 LLM에 물어봐서 얻는 답과, 이건 뭐가 다르죠?"로 풀어 썼습니다(EN도 같은 뜻으로).
  요청은 `dict.mentoring.groups` Day 7 sub 한 곳이었지만, 커밋 74dc1b8에서 같은 축약형이 들어간 `dict.faq` 답변도 함께 고쳤습니다.
  정본 자리 주석에 두 곳이 함께 움직일 것, 축약형으로 되돌리지 말 것과 그 이유를 남겼습니다. 다른 "프롬프트" 용례는 무관하여 유지했습니다.
- 검증: `npx tsc --noEmit` 0, `npm run build` 10/10; 축약형 카피 0건; 375px KO/EN 가로 오버플로 0.
- 원문: `git show bf718be:docs/changelogs/changelog-august-04-2026-prompt-question-clarify.md`

<a id="changelog-august-04-2026-en-copy-nits"></a>
## 2026-08-04 EN 카피 미세 수정 2건 + Day 1 시간 정정
- 범위: `data/dictionary.ts`, `data/schedule.ts`. EN 카피 2건은 KR 문자열 불변, Day 1 시간 정정은 ko/en 양쪽.
- 한 것: 영어에서 eyebrow와 리스트 제목이 같은 문구로 겹쳐 `whoWhat.whoTitle.en`을 `Who should join`에서 `This is for you if`로 바꿨다.
  `LAB2.en`의 페이즈 라벨을 행사 이름(Builderthon)에서 In action으로 바꿨다(KR 실전 유지, Lab 1의 Warm-up과 짝).
  Day 1 시간을 1PM to 5PM에서 1PM to 4:30PM으로 정정. The Foundry는 셋업과 철수가 대관 슬롯 안이고 *SCAPE는 밖이라 Day 1만 깎인다는 규칙을 주석에 적었다.
  8/13 사전 세션은 예약 창(18:00 to 20:00)을 그대로 공개하는 것이 의도임을 주석에 남기고 옛 표기(18:30 to 19:30)를 주석에서도 제거.
- 검증: `npx tsc --noEmit` 0, `npm run build` 10/10 정적 페이지. `grep "18:30|19:30"` 0건, 셋업/철수 관련 문자열은 공개 카피 0건.
- 원문: `git show bf718be:docs/changelogs/changelog-august-04-2026-en-copy-nits.md`

<a id="changelog-august-03-2026-goods-brandboost"></a>
## 2026-08-03 브랜드부스트 굿즈 확정
- 범위: `data/dictionary.ts`, `data/schedule.ts`. 컴포넌트 변경 없음.
- 한 것: 8/3 미팅 확정 사항 반영: 굿즈는 브랜드부스트가 제공하는 후드와 캡 세트 60개, Day 1(8/22) 전 도착, Day 1 현장 방문자 선착순 배포. 확정이라 헤지를 붙이지 않았고 배송지, 비용 등 물류 정보는 쓰지 않았다.
  FAQ의 "밥, 굿즈, 네트워킹은 전원에게"가 과약속이 되어 "밥과 네트워킹은 전원에게"와 굿즈 선착순 60세트 문장으로 나눴다.
  수정 4곳: benefits 06 줄, FAQ 상금 답변(ko, en), `schedule.ts` Day 1(`days[0].summary`, `d1-orientation`의 summary와 description), `partnerIntros["Brand Boost"]` 마지막 문장.
- 검증: `npx tsc --noEmit` 0, `npm run build` 10/10 정적 페이지. "전원"과 "굿즈"가 한 문장에 남은 곳 없음(KR, EN).
- 원문: `git show bf718be:docs/changelogs/changelog-august-03-2026-goods-brandboost.md`

<a id="changelog-august-03-2026-day5-networking-pivot"></a>
## 2026-08-03 Day 5 → 네트워킹 데이
- Scope: `data/schedule.ts`, `data/dictionary.ts`, `components/journey/Journey.tsx` (comment only). All copy `{ko, en}`.
- What: Feedback settled that Day 5 (8/26, *SCAPE) is for student-to-student networking, with the programme designed together with Hashed and nothing agreed yet.
  Removed five on-site Day 5 sessions (`d5-kickoff`, `d5-panel-usecase`, `d5-panel-founding`, `d5-networking`, `d5-quickathon`) and added one placeholder `d5-networking-day` with `confirmed` deliberately not set; `d5-fde-office-hour` kept as a separate online track.
  The old 10AM to 2PM frame was dropped; the modal chip uses `dayLabel` ("시간 확정 예정") instead of a bare "AM". `days[5]` theme became 네트워킹 (기획 중), and `HASHED_ORG.desc` moved to the networking day with a hedge.
  Derived surfaces synced: route strip keyword, `dict.program.modeNote`, and the Day 5 AI Use Case prize line removed from benefits 06 and the FAQ (no session, no prize).
- Verified: grep for removed terms leaves only historical comments; `npm run build` compiled, 10/10 static pages.
  Day 5 card reads 네트워킹 (기획 중) with 2 sessions; route strip node reads Day 5 네트워킹.
- 원문: `git show bf718be:docs/changelogs/changelog-august-03-2026-day5-networking-pivot.md`

<a id="changelog-august-03-2026-day1-venue-foundry"></a>
## 2026-08-03 Day 1 장소 확정, SMU YPHSL → The Foundry
- 범위: `data/schedule.ts`(Day 1 전체), `components/EventModal.tsx`(장소 줄 링크). 대관 비용과 내부 조건은 사이트에 쓰지 않음.
- 한 것: Day 1(8/22) 장소를 The Foundry의 The Refinery 홀(11 Prinsep Link)로 확정 표기. 상수 `YPHSL_B203`을 `FOUNDRY_REFINERY`로 교체해 Day 1 여섯 이벤트가 모두 참조.
  지시문의 시간 값이 플레이스홀더로 비어 있어 기존 11AM~5PM(SMU 예약 기준)을 빼고 "시간 확정 예정"으로 헤지, `TODO` 주석 남김.
  venue 참조만 바꾸고 전역 치환은 하지 않음(8/13 사전 세션의 SMU 장소, 주관 단체, 등록 폼 학교 선택지 등은 그대로).
  `BEvent`에 선택 필드 `locationUrl`을 더해 있을 때만 모달 장소 줄이 외부 링크(`https://foundry.sg`)가 되고, 없으면 기존과 동일.
- 검증: `npx tsc --noEmit` 0, `npm run build` 10/10 static pages. 1440px KR/EN 렌더 확인, 링크 속성 확인, 콘솔 에러 0.
- 원문: `git show bf718be:docs/changelogs/changelog-august-03-2026-day1-venue-foundry.md`

<a id="changelog-august-03-2026-benefits-peer-networking"></a>
## 2026-08-03 혜택 04 또래 네트워킹
- Scope: `data/dictionary.ts`, `dict.benefits.items[3]` (04 네트워킹) `points` only.
- What: Every point on the networking card pointed upward (founders, seniors, speakers, panels) and meeting other students appeared nowhere, while Day 5 had been re-pointed at peer networking. The three points were rewritten in the order peers, Day 5, seniors.
  Order matters because `BenefitCard` shows only the first two points below `sm`, so on a phone the card now reads as peers plus Day 5. The old three senior lines were compressed into one, keeping the Days 1, 5, 7 and 8 list as it was.
  Point 2 tracks `schedule.ts` wording (being planned, not confirmed). No community promise and no headcount were added; the spine and other cards are unchanged.
- Verified: `npx tsc --noEmit` 0, `npm run build` 10/10 static pages. At 375px the collapsed card shows exactly the peer and Day 5 lines, horizontal overflow 0.
- 원문: `git show bf718be:docs/changelogs/changelog-august-03-2026-benefits-peer-networking.md`

<a id="changelog-august-03-2026-nav-breakpoint"></a>
## 2026-08-03 데스크톱 내비 브레이크포인트 `lg` → `xl`
- Scope: `components/journey/JourneyNav.tsx` and `app/globals.css` (`scroll-padding-top` band).
- What: The reported EN 1280px lockup overlap was the mildest symptom of the desktop nav row overflowing in both locales across the whole `lg` to `xl` band (needs 1139px KR, 1201px EN); at 1024px the wordmark was crushed from 171px to 50px and the toggle sat off-screen.
  The inline anchor row, section rail, quiz chip, bar height and `scroll-padding-top` band now switch at `xl` (1280) instead of `lg`, so 1024 to 1279 gets the two-row treatment. Open Chat and Register buttons and the tablet bottom bar were deliberately left at `lg`.
  The EN-only brand suffix returns at `min-[1500px]`, the first measured width with real air (75px between the groups).
- Verified: `npx tsc --noEmit` 0, `npm run build` 10/10. Document horizontal overflow 0 at 375, 1024, 1100, 1280, 1400, 1440 and 1500; anchor jump at 1100px clears the header.
- 원문: `git show bf718be:docs/changelogs/changelog-august-03-2026-nav-breakpoint.md`

<a id="changelog-august-03-2026-partner-logo-uniform"></a>
## 2026-08-03 확정 파트너 스트립, 티어별 고정 박스
- Scope: `components/journey/Journey.tsx`, the hero confirmed-partner strip only (`confirmedPartnerTiers`, `StripLogo`, `HeroPartnerStrip`)
- What: Within a tier, wide wordmarks looked larger than crests because the equal-area rule (`opticalHeight()`) let width float on a strip with no visible chips.
  Each tier now has one fixed `StripBox` (`LEAD_BOX` 34x126, `SPONSOR_BOX` 26x98, with phone values) and every mark is `object-contain`-ed inside it. No per-logo overrides; `w`/`h` are aspect ratio only.
  Mobile shares the same rule; `STRIP_M_*` constants deleted. `opticalHeight()` is kept for the partner wall and Zero100 marquee, which draw marks inside visible containers.
- Verified: `npx tsc --noEmit` 0, `npm run build` 10/10 pages. At 375px DRIMAES no longer sits alone on a row (주최 wraps 3 + 2); horizontal overflow 0 at 375 and 1280.
- 원문: `git show bf718be:docs/changelogs/changelog-august-03-2026-partner-logo-uniform.md`

<a id="changelog-august-03-2026-partner-strip-static"></a>
## 2026-08-03 mobile partner strip to static
- Scope: `components/journey/Journey.tsx` (`HeroPartnerStrip`, `StripLogo`, hero composition), `app/globals.css` (`.marquee-hero` removal). `confirmedPartnerTiers` data unchanged.
- What: The mobile auto-scroll marquee was replaced by the same static tiered wrap grid desktop uses, because a logo wall earns trust by being seen at once.
  Mobile gets its own equal-area height clamp (lead tiers 21 to 27px, sponsors 18 to 24px) instead of a width cap that letterboxed three marks; `StripLogo` writes `--sl-h` / `--sl-h-sm` on one `<img>`.
  Deleted the marquee items, `looped` state and `.marquee-hero` (including its reduced-motion exemption); shared marquee classes stay for `#companions`.
  On mobile the hook cards moved below the partner strip so logos come before the signup ask; desktop untouched.
- Verified: mobile height range 12~32px to 18~27px (spread 2.67x to 1.50x), 0 letterboxed marks; desktop 18/18 mark heights identical; `npm run build` 10/10 static pages.
- 원문: `git show bf718be:docs/changelogs/changelog-august-03-2026-partner-strip-static.md`

<a id="changelog-august-03-2026-horizontal-overflow"></a>
## 2026-08-03 horizontal overflow
- Scope: `components/journey/Journey.tsx` (hero strip glow layer), `app/globals.css` (root overflow guard).
- What: At 375px the document had 18px of horizontal overflow despite `body { overflow-x: hidden }`. A full diagnostic (filtered to elements with no clipping ancestor) found only two offenders.
  The hero partner marquee was removed (replaced by a static grid, see the partner-strip entry). The glow layer bleed was matched to the rail padding (`-inset-x-6` below `sm`, `sm:-inset-x-10`) instead of clipping it.
  Added `overflow-x: clip` on both `html` and `body` as a safety net only (`clip`, not `hidden`, so sticky and programmatic scroll are not affected), with a comment not to hide new overflow behind it.
  Resolves the known issues noted in the station-principle and mobile-audit-fixes changelogs.
- Verified: `scrollWidth === clientWidth` at 375 and 320, document overflow 0, forced `scrollLeft` causes no movement; `npm run build` compiled, 10/10 static pages.
- 원문: `git show bf718be:docs/changelogs/changelog-august-03-2026-horizontal-overflow.md`

<a id="changelog-august-03-2026-mobile-audit-fixes"></a>
## 2026-08-03 mobile audit fixes
- Scope: `components/journey/Journey.tsx` (ScrollToTop, RouteMap, mentor and judge grids, benefit cards, band tint, `#launch` spacing), `data/dictionary.ts` (`dict.benefits.expand` / `.collapse`).
- What: ScrollToTop FAB never actually hid on scroll-down because framer-motion's inline `opacity` beat the class; fixed with `max-lg:!opacity-0`. Mobile visual disc shrunk to 36px inside a 50px touch target; desktop unchanged.
  Route map split into two rows of four on mobile so Day 8 is visible without scrolling; desktop still reads as one line.
  Mentor and judge grids became snap-scroll rows on mobile (same wrap grid from `sm` up); new `BenefitCard` collapses to two bullets on mobile with a "더 보기 / 접기" toggle (`aria-expanded`, `aria-controls`).
  Band tint replaced with a single self-fading gradient (the old edge fade made the seam darker); `#launch` bottom padding reduced.
- Verified: mobile page height 30,115px to 26,011px (-13.6%), `#mentoring` -3,921px; desktop regression checks all pass; `npx tsc --noEmit` 0, `npm run build` 10/10 static pages.
- 원문: `git show bf718be:docs/changelogs/changelog-august-03-2026-mobile-audit-fixes.md`

<a id="changelog-august-03-2026-station-principle"></a>
## 2026-08-03 정거장 원칙
- Scope: `components/journey/Journey.tsx` (`#program`), `data/dictionary.ts` (`dict.program`); `data/schedule.ts` read only (`days[].mandatory` is the single source).
- What: Two required days and six optional ones were stated only in prose, while eight equal cards in a 4x2 grid read as eight equal obligations.
  Layer A: `RouteMap` strip with eight nodes (Day 1 and 8 as filled rose nodes with a `필참` pill, keywords derived via `stopKeyword()`), a principle line and `route.optionalValue` saying the six stops are worth choosing (keep the two adjacent). On mobile it scrolls instead of restacking.
  Layer B: `선택` / `Optional` pill on non-required cards, rose border tint on Day 1 and 8. Layer C: `ProgramStats` (필참 2일, 선택 6일); a third "온라인 중심" stat was dropped because it was false (4 on-site / 4 online).
  Fixed `bg-white/12` dividers that compiled to nothing (now `bg-white/[0.14]`). A pre-existing 18px overflow at 375px was resolved the same day in a separate change.
- Verified: `npx tsc --noEmit` 0, `npm run build` 10/10 static pages; KR and EN desktop, KR at 375x780 (anchors 32px, optional 14px, strip scrolls).
- 원문: `git show bf718be:docs/changelogs/changelog-august-03-2026-station-principle.md`

<a id="changelog-august-02-2026-background-consistency"></a>
## 2026-08-02 background consistency
- Scope: `components/journey/Journey.tsx` (section tint bands) and `lib/background/{renderer/PostFX.ts, particles/ParticleField.ts, shaders/particles.vert.ts, shaders/particles.frag.ts}`. Hero and closing staging untouched.
- What: Section tints collapsed from two opacities (`/45`, `/55`) and two fade heights to one token `BAND_TINT = "bg-[#0a0814]/50"` with `<BandFades />` at h-24. Sections are now either BASE (no tint) or BAND; a third opacity is treated as a bug.
  WebGL phase amplitudes were reduced (bloom, lens strength, inward/swirl displacement, `vSpeed`, point size and cap 34px to 22px, trail alpha, heat) and the `ParticleField` opacity ramp changed from `.8 → .30 → .12` to `.8 → .34 → .22`, so the lower page no longer brightens into large soft discs. The phase system and `prefers-reduced-motion` damping are intact.
- Verified: background-only luminance range across the page 1.27x before, 1.18x after (noise floor 11.9%). The `#program` to `#speakers` seam fades with no visible edge.
- 원문: `git show bf718be:docs/changelogs/changelog-august-02-2026-background-consistency.md`

<a id="changelog-august-02-2026-mobile-openchat-cta"></a>
## 2026-08-02 mobile: open-chat CTA restored
- Scope: `components/journey/Journey.tsx` (hero CTA row, `MobileStickyBar`, `MobileRegisterBar`) and `data/dictionary.ts` (`stickyBar`).
- What: On phones open chat had no visible entrance, so it was restored in three places, all reusing `links.openChat` and the `openchat_click` event with a `src` tag.
  Phone sticky bar is now `[오픈채팅] [등록하기]` (icon plus text label, open chat first). The quiz chip and its `stickyBar.quiz` key were removed, since the quiz already has two phone entrances.
  A labelled ghost open-chat pill was added to the hero CTA row below lg (`lg:hidden`, `src: "hero"`). The tablet bar got the same label and changed to `hidden sm:block lg:hidden` so it no longer renders under the phone bar.
- Verified: `npx tsc --noEmit` and `npm run build` pass. At 375px both sticky buttons are 54px on one line, and there is exactly one fixed bottom bar on a phone (was two).
- 원문: `git show bf718be:docs/changelogs/changelog-august-02-2026-mobile-openchat-cta.md`

<a id="changelog-august-02-2026-flow-strip-mobile-arrows"></a>
## 2026-08-02 flow strips: mobile arrow direction and box widths
- Scope: `components/journey/Journey.tsx`, the "최종 아웃풋" strip in the programme section and the "참여 플로우" strip in the benefits section. No copy changed.
- What: Each arrow lived inside its box's flex row, so on phones the arrow sat beside its box pointing right and the boxes with arrows were narrower than the last (297px vs 321px at 375px).
  Children are now flat (`[box, arrow, box, arrow, box]`) in a `flex-col sm:flex-row` container; one glyph is rotated with `rotate-90 sm:rotate-0` so it points down on phones.
  Both strips now share a small `FlowStrip` component so the layout rule lives in one place.
- Verified: `npx tsc --noEmit` and `npm run build` pass; at 375px boxes measure `[321, 18, 321, 18, 321]` with arrows centred; at 1440px layout unchanged from before.
- 원문: `git show bf718be:docs/changelogs/changelog-august-02-2026-flow-strip-mobile-arrows.md`

<a id="changelog-august-02-2026-mobile-chrome-autohide"></a>
## 2026-08-02 mobile chrome: auto-hide + slimming
- Scope: `lib/useScrollDirection.ts` (new), `components/journey/JourneyNav.tsx`, `components/journey/Journey.tsx` (`MobileStickyBar`, `MobileRegisterBar`, `ScrollToTop`), `app/globals.css`. Desktop (lg and up) unchanged.
- What: On phones (especially in-app browsers) the two-row header, bottom register bar and back-to-top button took too much of the viewport.
  One shared scroll-direction hook (12px hysteresis, 80px top zone, visible while a modal locks scroll) now drives all chrome together.
  Header row slimmed below lg (145px → 105px, 44px chip targets kept) and slides away on scroll-down; `scroll-padding-top` 160px → 120px.
  Both bottom bars slide out on the same signal and stand down over the closing section; `body` bottom padding fixed at 4rem. The FAB hides with them below lg.
- Verified: `npx tsc --noEmit` and `npm run build` pass. 375×600: chrome hides on scroll-down, 6px jitter up keeps it hidden, 250px up restores all three.
  1440×900 identical to before; 375×812 FAB to bar gap 16px.
- 원문: `git show bf718be:docs/changelogs/changelog-august-02-2026-mobile-chrome-autohide.md`

<a id="changelog-august-01-2026-mentoring-two-groups"></a>
## 2026-08-01 mentoring: two theme groups
- Scope: `components/journey/Journey.tsx` (`<Chapter id="mentoring">`) and `data/dictionary.ts` (`dict.mentoring`). Judges and all mentor data untouched.
- What: Replaced the three stage cards plus click-to-filter grid (shipped earlier the same day) with physical grouping: a warm-up strip (Day 1 to 2 session hosts), a "빌드 멘토링 Day 3~6" box and a "피치, 세일즈 멘토링 Day 7" box.
  The build box header shows the two main mentoring partners (Onword Lab, Popup Studio) as logos with span chips; the Popup Studio chip reads Day 5~7 to match `schedule.ts`, plus a dashed FDE office-hours placeholder card. The pitch box deliberately has no partner logos.
  `dict.mentoring.stages` became `dict.mentoring.groups` plus `dict.mentoring.warmup`; `mentors[].stages` is reused as the join key, so no counts or name lists live in the component. Filter state and its keys were removed.
  `separationNote` moved to the top of the section; `matchNote` stays below both boxes.
- Verified: `npx tsc --noEmit` and `npm run build` pass; at 1440 and 375 in both locales, warm-up 2 people, build box 6 cards + placeholder, pitch box 5, no horizontal overflow at 375px.
- 원문: `git show bf718be:docs/changelogs/changelog-august-01-2026-mentoring-two-groups.md`

<a id="changelog-august-01-2026-mentor-stage-highlight"></a>
## 2026-08-01 mentoring: stage → mentor highlight
- Scope: `components/journey/Journey.tsx` (`<Chapter id="mentoring">`) and `data/dictionary.ts` (`dict.mentoring`). Judges subsection and mentor data untouched except a derived `stages` field.
- What: The stage cards and the mentor grid never referenced each other, so the stage cards now filter the grid; matchNote moved below the grid because a filter has to sit next to what it filters.
  Added `dict.mentoring.mentors[].stages: number[]`, derived from each mentor's existing `days` / `daysPending`. No mentor carries stage 2 (Day 5 to 7 is a drop-in office hour), so selecting it shows `dict.mentoring.stageNoMentors`.
  Stage cards became `<button aria-pressed>`; matches get an emerald highlight, the rest drop to `opacity-45`. Click (`activeStage`) wins over mouse-only hover preview (`hoverStage`), an active-filter chip shows the clicked filter, and hovering a mentor lights their stage card.
  With `useReducedMotion` on, transitions are dropped.
- Verified: `npx tsc --noEmit` and `npm run build` pass. Stage 1 gives 6 lit / 7 dimmed, stage 3 gives 5 lit / 8 dimmed, clearing restores 13 lit.
  No horizontal overflow at 375px; dimmed opacity 0.45 is above the 0.35 legibility floor.
- 원문: `git show bf718be:docs/changelogs/changelog-august-01-2026-mentor-stage-highlight.md`

<a id="changelog-august-01-2026-output-framing"></a>
## 2026-08-01 final-output & brief framing
- Scope: content only in `data/dictionary.ts` and `components/journey/Journey.tsx` (`#program` head), four surfaces, no schedule data touched; every string a `{ko, en}` pair.
- What: The site never said what a team hands in or what the problem looks like when it arrives. Per the 7/29 to 7/31 organizer meetings, the final output is three parts (the client's workflow, the bottleneck and AI fix as the idea, a demo as evidence), and the problem arrives as an AX brief on Day 1.
  Added a "최종 아웃풋" block (`program.outputTag` / `outputHeading` / `outputSteps` / `outputNote`) at the head of the programme section; step 3 carries "persuasive, not polished".
  Added one AX-brief line to benefits card 02, one sentence to the theme FAQ, and opened the judging FAQ with the deliverable (weightings unchanged).
  Deliberately not written: presentation time split, new track-count emphasis, presenter names or KorCham mention, new vocabulary.
- Verified: `npx tsc --noEmit` and `npm run build` pass; output strip checked at 1440 and 390 px in both locales (three stacked rows on mobile, no overflow).
- 원문: `git show bf718be:docs/changelogs/changelog-august-01-2026-output-framing.md`

<a id="changelog-july-22-2026-cta-hierarchy-partner-strip"></a>
## 2026-07-22 등록 CTA 위계, 확정 파트너 스트립, 카카오톡 전환
- Scope: `JourneyNav.tsx`, `Journey.tsx`, `app/globals.css`, `data/dictionary.ts`, `RegisterModal.tsx`, `lib/RegisterContext.tsx`, `app/api/register/route.ts`, `lib/telegram.ts` renamed to `lib/kakao.ts`.
- What: Made registration the primary action: a glowing gradient pill in the nav, the whole hero card clickable, and a mobile sticky register bar once `#about`'s top passes.
  Added a confirmed-partner logo strip in the hero (host, organizers, sponsors), settled as a vertical tiered stack at 22 to 38px with the scroll fade removed; fixed a reduced-motion bug that left 13 of 17 partners off-screen on phones.
  Added conversion microcopy (`register.reassure`, a 3분 chip, an honest urgency line with no fabricated deadline) and deleted `register.hookFacts`; "필수는 3칸" corrected to 4.
  "등록 완료" now opens an already-registered panel pointing to the organizers and the open chat (no self-serve edit, no schema change). Contact switched from Telegram to KakaoTalk with a rewritten normaliser; no DB change.
- Verified: LCP back to 939 ms (baseline 890 to 967 ms) after `fetchPriority="low"` and deferred marquee duplication, CLS 0.00; `tsc` and `build` clean, Kakao normaliser 10 unit cases.
- 원문: `git show bf718be:docs/changelogs/changelog-july-22-2026-cta-hierarchy-partner-strip.md`

<a id="changelog-july-21-2026-section-trim-partner-sync"></a>
## 2026-07-21 섹션 정리 + 파트너 월 덱 싱크
- Scope: `data/dictionary.ts`, `components/journey/Journey.tsx`, `scripts/process-partner-logos.py`, `public/partners/logos/white/`
- What: Organizer requests from screenshots: removed the '상금, 시상' incentive card (prizes still under negotiation) and the whole `#why-partner` traction section (with `CountUp`); the '파트너' nav anchor now points to `#builders`.
  Added a Day 7 career session speaker card (heading now Day 1, 5, 7, 8); list keys switched to index to avoid a duplicate React key. Partner wall now matches the deck: one confirmed row with role captions, '협의 중' and separate mentor tiers removed.
  Deck partner logos added to the marquee; new script converts logos to white mono and trims transparent padding for the marquee. A BZCF press row was added inside the 지금의 현실 block as the page's only external link.
  Notes flagged for the user: `companionsSub` was widened, and promoting some sponsors to 확정 should be checked against actual agreements.
- Verified: `npx tsc --noEmit` clean, `npm run build` 9/9 pages; `why-partner` appears nowhere outside a comment.
- 원문: `git show bf718be:docs/changelogs/changelog-july-21-2026-section-trim-partner-sync.md`

<a id="changelog-july-21-2026-deck-content-sync-2"></a>
## 2026-07-21 덱 → 웹사이트 콘텐츠 싱크 2차
- Scope: `data/schedule.ts` and `data/dictionary.ts` only (content). Registration path and `/quiz` untouched.
- What: Second sync against the latest deck. Day 5 rebuilt as a mid-point check-in and exchange day (theme 중간 점검 및 교류) with the student AI use-case showcase and QR vote, a "유학생에서 창업가로" panel (still being arranged, `speaker` field removed), an award, and cross-track exchange.
  Day 7 time corrected to 9AM to 2PM and its speaker session replaced by a career session; Day 8's career session deleted (now 3 sessions). The speakers section reduced to two cards and a Day 5 panel note.
  Track vocabulary renamed Normal / Beginner to 딥다이브 / 스프린트; an AI Use Case award bullet added; hero location now includes the AWS office. Tentative wording was preserved throughout, nothing promoted to confirmed.
- Verified: residue grep for removed names and times returns zero hits; invariants (dates, team 1 to 3, figures) unchanged; `npx tsc --noEmit` and `npm run build` (9/9) clean.
- 원문: `git show bf718be:docs/changelogs/changelog-july-21-2026-deck-content-sync-2.md`

<a id="changelog-july-20-2026-supabase-backend"></a>
## 2026-07-20 Supabase 등록 백엔드
- Scope: `supabase/migrations/0001_registrations.sql`, `lib/supabaseAdmin.ts`, `app/api/register/route.ts` (all new), `data/dictionary.ts`, `components/RegisterModal.tsx`, `.env.local.example`, `.gitignore`.
- What: The registration form had been posting nowhere. It now POSTs to `/api/register`, which re-validates server-side and writes one `registrations` row plus one `registration_members` row per person, with a `registration_participants` view.
  RLS on both tables with zero policies; writes use the `service_role` key via a `server-only` client that never reaches the browser.
  Bug fix in `RegisterModal`: it showed success even when the submit failed; now only a 2xx advances, otherwise an inline retry alert.
  Later superseded (July 22): the contact channel moved from Telegram to KakaoTalk. No rate limiting or spam protection yet.
- Verified: `npx tsc --noEmit` clean, migration applied; local E2E team of 3 and solo returned `201`, missing email returned `400`, test rows cleaned up (cascade confirmed).
- 원문: `git show bf718be:docs/changelogs/changelog-july-20-2026-supabase-backend.md`

<a id="changelog-july-18-2026-reset-helper"></a>
## 2026-07-18 ?reset=1 QA helper
- Scope: new `lib/storage.ts` and `components/ResetHandler.tsx`; `lib/quizResult.ts`, `lib/RegisterContext.tsx`, `components/RegisterModal.tsx`, `components/Quiz.tsx`, `app/page.tsx`, `app/quiz/page.tsx`, `dict.resetToast`.
- What: Visiting any page with `?reset=1` wipes every `z100-*` key from localStorage and sessionStorage, shows a confirmation toast and strips the param, for testing fresh-user flows on the deployed site including phones.
  `lib/storage.ts` holds all site storage keys and `clearSiteStorage()` (a prefix sweep); the locale pref `builderthon.locale` is deliberately kept. Other files now import keys from it.
  `ResetHandler` is the first child on `/` and `/quiz` so it runs before other effects read storage, so no second refresh is needed.
- Verified: `npx tsc --noEmit` clean, `npm run build` 9/9 pages. Live QA: `/?reset=1` and `/quiz?reset=1` clear `z100-*` keys and keep the locale and other apps' keys, with no console/hydration errors.
- 원문: `git show bf718be:docs/changelogs/changelog-july-18-2026-reset-helper.md`

<a id="changelog-july-18-2026-solo-matching-ai-type"></a>
## 2026-07-18 solo-matching AI-type attach
- Scope: `components/RegisterModal.tsx`, `data/dictionary.ts`, `components/Quiz.tsx`, `data/quiz.ts`, `lib/RegisterContext.tsx`.
- What: The AI-type attach is now only for solo applicants who opt into matching. Join type is reduced to team and solo; team applicants never see AI-type UI and never send a `quizType`.
  Solo shows a matching checkbox; when checked, State A confirms a saved result, State B prompts the test. A quiz round-trip saves the draft to `sessionStorage` (`z100-register-draft`), goes to `/quiz?return=register`, and after a genuine completion a banner returns to the modal with fields restored.
  Payload carries `wantsMatching`, and `quizType` only when matching and a saved result exists. The AI type is read only from this device's saved result; the `?type=` URL param was dropped.
- Verified: `npx tsc --noEmit` clean, `npm run build` 9/9 pages. Team, solo and full round-trip paths checked live with correct payloads and no console errors.
- 원문: `git show bf718be:docs/changelogs/changelog-july-18-2026-solo-matching-ai-type.md`

<a id="changelog-july-18-2026-team-registration"></a>
## 2026-07-18 team registration
- Scope: `components/RegisterModal.tsx`, `data/dictionary.ts`
- What: One person can now register a team of 1~3. Join type (`team` / `looking` / `solo`) reveals a team section with a required team name; the registrant is Member 1 and up to two more members can be added or removed inline, with clean renumbering.
  Optional LinkedIn field for everyone; team name required for `team`, every member email format-checked, no duplicate emails across members. The old solo-matching checkbox was removed.
  Payload reshaped around a `members` array (`members[0]` is the registrant; `quizType` stays top-level); modal subtitle mentions the team option. All copy bilingual.
- Verified: `npx tsc --noEmit` clean, `npm run build` 9/9 pages. Live QA: team-of-3 submit produced the expected payload; duplicate email blocked submit; no console or hydration errors.
- 원문: `git show bf718be:docs/changelogs/changelog-july-18-2026-team-registration.md`

<a id="changelog-july-18-2026-register-flow"></a>
## 2026-07-18 register flow
- Scope: `components/journey/Journey.tsx`, `components/journey/JourneyNav.tsx`, new `components/RegisterModal.tsx` and `lib/RegisterContext.tsx`, `app/page.tsx`, `data/dictionary.ts`, `components/Quiz.tsx`.
- What: Hero gets two question-hook cards (need a team, go to the personality test; solo or already have a squad, register), replacing the standalone quiz button.
  The nav register button now appears only once `#about` is reached (IntersectionObserver) and then persists, reading "등록 완료 ✓" after registering.
  New 6-field register modal (name, email, contact required) reusing the PartnerModal pattern, with quiz-type auto-attach, `REGISTER_ENDPOINT` submit (empty for now, simulated), `RegisterContext` owning the single modal and the `?register=1` auto-open.
  Team size reverted to 1 to 3 (organizer override of the deck's 1 to 5). Superseded on July 22: the contact channel moved from Telegram to KakaoTalk.
- Verified: `npx tsc --noEmit` clean, `npm run build` 9/9 pages. Live QA of all flows passed with zero hydration warnings.
- 원문: `git show bf718be:docs/changelogs/changelog-july-18-2026-register-flow.md`

<a id="changelog-july-18-2026-deck-content-sync"></a>
## 2026-07-18 deck content sync
- Scope: `data/schedule.ts`, `data/dictionary.ts` (`dict.speakers` and others); content only, no scenes, animation, layout or component logic.
- What: The site had drifted a version behind the revised deck; five passes, all in both `ko` and `en`, with unconfirmed items kept at "조율 중 / 잠정 / TBC".
  Realigned the speaker rotation (each of three speakers was one slot off) and Day 1 details (`dayMode` to `pending`, track selection added).
  Day 2 problem video became live per-track briefings; Day 3 and 4 mentoring moved to NUS in person with confirmed mentors under a public-naming TODO; Day 7 and Day 8 timetables reshaped to the deck.
  Tracks 2 to 3 (재무, 영업, 마케팅(입문)), award structure spelled out with a mandatory "잠정, 조율 중" label, team size 1 to 5, and a judging-criteria FAQ added.
- Verified: `npx tsc --noEmit` clean, `npm run build` 9/9 static pages, `verify-quiz.mjs` invariants hold.
- 원문: `git show bf718be:docs/changelogs/changelog-july-18-2026-deck-content-sync.md`

<a id="changelog-july-15-2026-quiz-v10"></a>
## 2026-07-15 quiz phase 10
- Scope: `components/Quiz.tsx` (`StoryCard`), `data/quiz.ts`
- What: The 1080x1920 story card now shows two axis-explanation highlight cards under the gauges: the highest-% axis (violet) and the lowest-% axis (cyan), using the same `getExplanation` text as the result screen.
  Body text 31px, full text with no truncation; the identity and gauge block was tightened to make room. If the bottom CTA is pushed past the safe line (1740), the second card is dropped.
  Deep-link (`?r=`) visitors with no `axes` get the original card unchanged; missing explanations or duplicate hi/lo axes are skipped.
- Verified: In-browser checks on multiple results; cards match the highest/lowest gauges and fit at the safe line; `npx tsc --noEmit` passes.
  The `html-to-image` PNG could not be produced in the automation harness (pre-existing hang, unrelated); a manual save on a preview/prod build is recommended.
- 원문: `git show bf718be:docs/changelogs/changelog-july-15-2026-quiz-v10.md`

<a id="changelog-july-15-2026-quiz-v9"></a>
## 2026-07-15 quiz phase 9
- Scope: new `lib/quizResult.ts` and `components/journey/ReturningGreeting.tsx`; `components/Quiz.tsx`, `components/journey/Journey.tsx`, `data/dictionary.ts`.
- What: The taker's own result id is saved to `localStorage` (`z100-quiz-result`, `{ v: 1, resultId, savedAt }`) so it survives a browser restart; the load re-validates the id and drops invalid keys.
  Saving happens only in `enterResult` (genuine completion), so a friend's `?r=` deep-link is never persisted; a completed retake overwrites.
  Returning takers see a hero greeting pill with their AI-model type, the hero quiz CTA becomes "내 결과 보기" (`nav.quizResult`), and the `/quiz` landing shows a "지난 결과" link. First-time visitors see the hero as before; greeting renders only after mount to avoid hydration mismatch.
- Verified: end-to-end browser QA (save, reload persistence, own-link vs friend link, retake overwrite); zero hydration/console errors, `npx tsc --noEmit` passes.
- 원문: `git show bf718be:docs/changelogs/changelog-july-15-2026-quiz-v9.md`

<a id="changelog-july-13-2026-quiz-v8"></a>
## 2026-07-13 quiz phase 8
- Scope: `components/Quiz.tsx`, `data/quiz.ts`.
- What: Restored the share-link and retake actions removed in phase 6.5, arranged around the phase-7 image button: "이미지로 저장" stays full-width as primary, with a row of "결과 공유하기" and retake below.
  Restored the `OWN_KEY` sessionStorage helpers and `fromShare` state so share-link (`?r=`) visitors see a highlighted "나도 테스트하기" while the taker sees plain "다시 테스트하기".
  The image save and the result-link `share` are deliberately separate actions; `share`, `copied`, `retake`, `retakeViral` copy restored.
- Verified: `npx tsc --noEmit` clean, `next build` succeeds with `/quiz` First Load JS 175 kB; manual QA of fresh visitor, own result and reload cases.
- 원문: `git show bf718be:docs/changelogs/changelog-july-13-2026-quiz-v8.md`

<a id="changelog-july-13-2026-quiz-v7"></a>
## 2026-07-13 quiz phase 7
- Scope: `components/Quiz.tsx` (`StoryCard`, `saveImage`), `data/quiz.ts` (save-image copy), `package.json` (`html-to-image`).
- What: The `/quiz` result screen can export a 9:16 (1080x1920) story image via a new "이미지로 저장 / Save as image" button; on mobile it goes to the native share sheet, on desktop it downloads.
  Built as a dedicated off-screen `StoryCard` so the export is pixel-stable; the axis-gauge block renders only when `result.axes` exists (deep-link visitors get the type-only card).
  `html-to-image` is dynamically imported, fonts awaited, and `toBlob` called twice as an iOS Safari workaround. The button sits below the dream-teammate section because the share/retake row was removed in phase 6.5.
- Verified: `npx tsc --noEmit` clean; `html-to-image` is a lazy chunk, `/quiz` First Load JS 174 kB; desktop Chrome exports 1080x1920 PNGs with and without gauges. Real-device QA pending.
- 원문: `git show bf718be:docs/changelogs/changelog-july-13-2026-quiz-v7.md`

<a id="changelog-july-13-2026-quiz-v6"></a>
## 2026-07-13 quiz phase 6
- Scope: `public/logos/`, `scripts/process-logos.py` (new), `data/quiz.ts`, `components/Quiz.tsx`, `scripts/verify-quiz.mjs`.
- What: Self-hosted real white-mono logos for the five models that showed an emoji fallback (Grok, Character.AI, Pi, MS Copilot, Midjourney).
  Raster originals were turned into transparent white-mono PNGs via a PIL script (corner background sampling, color distance to alpha, trim, 512×512); Grok's SVG was recolored and trimmed to the icon mark.
  The `logo` field now holds a full filename so PNG and SVG coexist; ChatGPT and Cohere keep `logo: ""` and their emoji fallback (per decision).
  `verify-quiz.mjs` now fails if a non-empty `logo` has no file.
- Verified: 14/16 logos have a file, 2 intentional emoji fallbacks; `npx tsc --noEmit` clean; all PNGs under 50 KB; manual QA of all five marks.
- 원문: `git show bf718be:docs/changelogs/changelog-july-13-2026-quiz-v6.md`

<a id="changelog-july-13-2026-quiz-v5"></a>
## 2026-07-13 quiz phase 5
- Scope: `data/quiz.ts`, `components/Quiz.tsx`, `lib/eventMatch.ts` (deleted), `components/journey/Journey.tsx`, `data/dictionary.ts`.
- What: Reworked the result screen to card, then CTA, then a "환상의 궁합 (Dream teammates)" section showing the two best-fit types and why (new `matchWhy` field, 32 ko/en reasons, new `DreamTeammates` component).
  Retired the session recommender entirely (`EventRecsPanel`, `lib/eventMatch.ts`, the orphaned `/?event=<id>#program` handler).
  Removed the share and retake row and its sessionStorage own-result machinery; `?r=` deep links still work.
  Added `links.signup` (placeholder to `#program` with a `TODO`) and a mid-page apply CTA carrying `?ref=quiz&type=<resultId>`.
- Verified: `npx tsc --noEmit` clean, `verify-quiz.mjs` confirms 16 x 2 = 32 aligned `matchWhy` phrases; manual QA of order, `?r=INTJ-A` deep link and CTA href.
- 원문: `git show bf718be:docs/changelogs/changelog-july-13-2026-quiz-v5.md`

<a id="changelog-july-13-2026-quiz-v4"></a>
## 2026-07-13 quiz phase 4
- Scope: `data/quizExplanations.ts`.
- What: All 36 axis-explanation phrases rewritten in a B-grade self-roast tone with recognizable behavioral details, keeping the same grammar and structure.
  Each still cites the highest-weight against-result scenario, ties to `{pct}` and ends on one emoji; the English side carries the same joke rather than a literal translation. Hedge wording stays locked to each pattern's score band.
- Verified: `verify-quiz.mjs` 36/36 keys with bands intact, `{pct}` present in both locales for all 36, `tsc --noEmit` clean.
- 원문: `git show bf718be:docs/changelogs/changelog-july-13-2026-quiz-v4.md`

<a id="changelog-july-12-2026-quiz-v3"></a>
## 2026-07-12 quiz phase 3
- Scope: `data/quiz.ts`, `lib/quizScore.ts`, `data/quizExplanations.ts` header, `scripts/verify-quiz-axes.mjs`, `scripts/verify-quiz.mjs` (branch `quiz-rebalance-v3`).
- What: Phase-2 Sidon weights tilted the weighted first-option mass to 13:36, so answering all first options always gave ISFP-T. Swapped a/b on Q1 and Q3 and reverted the phase-1 swaps on Q6 and Q8; every axis now within |diff| of 4.
  Gauge %s came from four fixed values per axis. Display % now stretches the win margin over each axis's band and adds a deterministic `spice()` in [-2, +2] (djb2 hash of the answer sheet plus axis id, no `Math.random`), so same answers give the same result.
  The 36 explanation phrases are unchanged; verify scripts assert the new invariants.
- Verified: `verify-quiz-axes.mjs` a-lead 23:26 (47%), `verify-quiz.mjs` bands at least 5 apart with no dip to 50 or below, `npx tsc --noEmit` clean.
- 원문: `git show bf718be:docs/changelogs/changelog-july-12-2026-quiz-v3.md`

<a id="changelog-july-12-2026-quiz-v2"></a>
## 2026-07-12 quiz phase 2
- Scope: `/quiz` surface: `data/quiz.ts`, `data/quizExplanations.ts`, `lib/quizScore.ts`, `components/Quiz.tsx`, `app/quiz/page.tsx`, `public/logos` (branch `quiz-sidon-v2`).
- What: Sidon-weighted scoring: per-axis question weights form Sidon sets so each axis shows 4 distinct % bands instead of 67%/100%, and ties (50%) are impossible. Two new questions bring the quiz to 14.
  Answer-aware explanations: new static `EXPLANATIONS[axis][pattern]` table (36 bilingual phrases) shown as single-open accordions on the gauges, citing the taker's own answers.
  Model rebalance: five type-to-model mappings re-grounded (INTP Meta Llama, INFP Character.AI, ENFP Pi, ESFJ Microsoft Copilot, ISTP Ollama), and a "왜 이 모델" line (`whyModel`) added to all 16 results.
  Existing `?r=<id>` share links still resolve.
- Verified: `npm run build` clean; new `scripts/verify-quiz.mjs` confirms distinct bands, no 50%, full `EXPLANATIONS` coverage and 14-question weights, all green.
- 원문: `git show bf718be:docs/changelogs/changelog-july-12-2026-quiz-v2.md`

<a id="changelog-july-12-2026"></a>
## 2026-07-12 AI personality test (`/quiz`) improvement pass
- Scope: quiz surface only (`app/quiz`, `components/Quiz.tsx`, `data/quiz.ts`, `lib/quizScore.ts`, `lib/eventMatch.ts`, `public/logos`), on branch `quiz-improvements`.
- What: Fixed an ENTJ-A skew: six questions now lead with the opposite pole (first-position tally 8:7 instead of 15:0).
  Refresh on `?r=` no longer treats your own result as a friend's share (own id kept in `sessionStorage`).
  Added per-axis % gauges on the result card, a ~2.4s "analyzing" interstitial, and A/T identity bonus in session recommendations.
  Self-hosted brand logos from the `simple-icons` package (dropped the simpleicons CDN), role × category recommendation copy, and progress-bar ARIA.
- Verified: `npm run build` clean; `scripts/verify-quiz-axes.mjs` weight sums 3, tally 8:7; manual QA incl. 0 simpleicons.org requests.
- 원문: `git show bf718be:docs/changelogs/changelog-july-12-2026.md`

<a id="changelog-june-15-2026"></a>
## 2026-06-15 Design-revamp polish cycle
- Scope: branch `choi-design-revamp`; mainly `components/journey/Journey.tsx`, `components/EventModal.tsx`, `components/journey/JourneyNav.tsx`, `app/layout.tsx`, `app/globals.css`, `next.config.mjs`, `package.json`.
- What: Content: "Why this exists" gained a sourced "The gap today" stat band; Program reframed as "Six days to learn together, not compete" with Day 2 to 5 side sessions marked optional.
  Polish: root font size 16px to 18px, partner logo normalization, UI polish and reduced-motion-safe micro-interactions; framer-motion animations now gated by `useReducedMotion()`, 44px tap targets, focus restore, ARIA and contrast fixes.
  Performance and launch: self-hosted Pretendard via `next/font/local`, deferred WebGL (skipped under reduced motion), unused `@react-three/*` deps removed, OG/Twitter cards, favicon, `robots.ts`, `sitemap.ts`, Vercel Analytics, baseline security headers (CSP deferred).
  Mobile fit down to 360px (`100dvh`, compact nav brand, vignette overflow clip) and a hero headline descender clipping fix. The violet WebGL hero was preserved throughout.
- Verified: every commit passed `tsc --noEmit` and `next build`; launch files served 200; at 360px `docScrollWidth == innerWidth`.
- 원문: `git show bf718be:docs/changelogs/changelog-june-15-2026.md`
