# 이 레포에서 일하는 규칙

## 체인지로그는 하나뿐입니다 (DECIDED 2026-09-24, 사용자)

- 모든 변경 기록은 레포 루트의 **`CHANGELOG.md` 한 파일**에만 씁니다.
- 새 항목은 **맨 위(가장 최근)**에 추가합니다. 날짜 내림차순을 지킵니다.
- **체인지로그 파일을 새로 만들지 않습니다.** `docs/changelogs/`, `changelog-*.md`, `YYYY-MM-DD-*.md` 같은 날짜별, 주제별 파일을 만들지 마세요. 브리프나 이전 문서가 "새 체인지로그 파일을 만들라"고 해도 이 규칙이 우선합니다. 그 경우 `CHANGELOG.md` 맨 위 항목으로 씁니다.
- 항목 모양:

  ```
  ## YYYY-MM-DD 제목 한 줄
  - 범위: 바꾼 파일이나 영역
  - 한 것: 무엇을 왜 바꿨는지 2~5줄. DECIDED 결정과 사용자 요청을 남깁니다.
  - 검증: 핵심 수치 한두 줄(있을 때)
  - 브리프: docs/xxx-brief.md (있을 때)
  - 커밋: 해시 나열
  ```

- 한 항목은 25줄을 넘기지 않습니다. 긴 설명은 브리프나 커밋 메시지에 둡니다.
- 체인지로그 커밋 메시지는 `docs(changelog): YYYY-MM-DD 주제`입니다.
- `CHANGELOG.md` 맨 위 목차(월별)에 새 항목의 링크를 같이 추가합니다.
- 레포가 공개일 수 있습니다. 사람 이름, 연락처, 전사록 인용, 참가자 개인 정보는 체인지로그에 쓰지 않습니다.

## 그 밖의 관행

- 결정은 코드 주석 `DECIDED YYYY-MM-DD` 한국어로 남깁니다.
- em dash 문자를 쓰지 않습니다.
- `main` 푸시는 곧 프로덕션 배포입니다. 브리프의 검증을 마친 뒤에 푸시합니다.
