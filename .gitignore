# dependencies
/node_modules

# next.js
/.next
/out
/build
next-env.d.ts
*.tsbuildinfo

# env / secrets
.env
.env.local
.env*.local
*.pem

# deploy
.vercel

# local agent/editor config
.claude

# os / editor
.DS_Store

# audit / scratch / temp artifacts
.audit
screenshots
*.log

# large source assets kept locally, not in the repo
# (the hero uses the extracted mp4/jpg in public/hero/, not these)
lib/metal-human.zip
/Gemini_Generated_Image_*.png
/public/hero-bg.png
