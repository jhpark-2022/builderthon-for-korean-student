// ─────────────────────────────────────────────────────────────────────────────
// 나루 홈의 제목 스케일. NaruHome과 RecordTabs가 함께 읽습니다.
//
// 여기 있는 이유는 순환 참조입니다. NaruHome이 RecordTabs를 import 하므로
// RecordTabs가 NaruHome에서 상수를 가져올 수 없습니다. 그렇다고 같은 문자열을
// 두 파일에 적어 두면 한쪽만 고쳐집니다.
// ─────────────────────────────────────────────────────────────────────────────

/**
 * 챕터 제목. 8월 페이지의 아홉 개 h2와 같은 clamp입니다
 * (Journey.tsx의 CHAPTER HEADING SIZE 주석). 두 페이지의 제목이 같은 크기로
 * 읽혀야 한 사이트입니다. clamp 값을 바꾸지 마세요.
 *
 * max-w는 2026-09-15에 붙었습니다. 1440px에서 긴 한글 h2가 1296px 레일을 그대로
 * 써서 한 줄이 1,000px을 넘었습니다. 그 아래 lead는 756px입니다. globals.css가
 * heading에 text-wrap: balance를 걸어 두었으니 폭만 주면 균형 잡힌 두 줄로
 * 떨어집니다.
 */
export const H2 =
  "mx-auto max-w-[52rem] text-[clamp(2rem,5.5vw,3.75rem)] font-bold tracking-tight text-white";

/**
 * 챕터 안의 하위 블록 제목.
 *
 * h2(최대 67.5px)와 기존 h3(20.25px) 사이에 아무것도 없어서 #record의 세 하위
 * 블록이 앉을 자리가 없었습니다. 탭 블록의 제목과 그 안쪽 구획 제목이 한 글자도
 * 다르지 않았어요. 이 계단이 생기면 안쪽 라벨이 두 단계 아래로 내려가 제 역할을
 * 합니다.
 */
export const H3 =
  "break-keep text-[clamp(1.35rem,2.6vw,1.9rem)] font-bold tracking-tight text-white";

/**
 * 구획 제목이 라벨의 모양을 유지해야 할 때.
 *
 * ADDED 2026-09-16. #why의 "약속이 지켜지는 지점"과 "방법은 바뀝니다"가 이
 * 자리입니다. 둘 다 그 아래 문단과 카드들을 이끄는 제목인데 <p>로 그려지고
 * 있었어요. 보고 있는 사람에게는 제목인 것이 보조기술에는 본문이면 그건
 * 1.3.1 위반입니다. 실제 증상도 있었습니다: 스크린리더로 H 키를 누르면 코어
 * 두 개를 지나 바로 문서 끝으로 떨어져서, 페이지의 마지막 문장인 "방법은
 * 바뀝니다"가 있다는 것조차 알 수 없었습니다.
 *
 * H3(24.3~34.2px)은 이 자리에 너무 큽니다. 라벨의 생김새(대문자 + 자간)는
 * 그대로 두고 태그만 heading으로 올리는 계단입니다.
 *
 * 색이 /50이 아니라 /75인 것은 대비 때문입니다. 12.24px은 큰 글자 예외를
 * 받지 못해 4.5:1이 필요한데, /50은 실제 배경 위에서 5.14:1로 여유가 10%
 * 남짓입니다. 제목으로 올리는 요소를 그 줄 위에 두지 않습니다. /75는 10.11:1.
 */
export const LABEL_HEADING =
  "text-[0.72rem] font-bold uppercase tracking-[0.16em] text-white/75";

/**
 * 챕터가 존재하는 이유인 문장. H2와 H3 사이의 한 단.
 *
 * ADDED 2026-09-17. #why의 코어 둘("안전하게 도전할 수 있는 자리", "자기 가치를
 * 증명해 보는 경험")이 이 자리입니다. 그 둘이 H3(최대 34px)로 카드 안에 있을 때,
 * 페이지에서 가장 중요한 두 문장이 카드 제목으로 읽혔습니다. 약속의 내용이
 * 약속(H2, 최대 67.5px)보다 작으면 안 되지만, 같으면 제목이 둘이 됩니다.
 *
 * 2026-09-17 (2차): clamp(1.75rem,3.8vw,2.875rem)에서 한 단 내렸습니다. 27~38px.
 * 첫 판형은 51.75px까지 갔는데 사용자가 "또 너무 큰데"라고 했고, 실제로 H2
 * (67.5px)와 겨루는 크기였습니다.
 *
 * 2026-09-17 (3차): 22.5~29px. 사용자가 코어 제목 둘을 짚어 "font가 너무 크다"고
 * 했습니다. 이제 H3(24~34px)보다 작습니다. 이 토큰이 하는 일은 "크다"가 아니라
 * "판의 첫 줄"이라는 자리 표시이고, 판의 무게는 제목이 아니라 그 아래 첫 줄과
 * 오른쪽 열의 배치가 나릅니다. 더 줄이면 첫 줄(text-lg sm:text-xl)과 겹칩니다.
 *
 * leading이 1.15인 이유: 두 줄로 접히는 한글 제목이 tracking-tight에서 줄 사이가
 * 벌어져 보이지 않게. H2는 globals.css의 heading 규칙이 같은 값을 줍니다.
 */
export const STATEMENT =
  "break-keep text-[clamp(1.25rem,2vw,1.625rem)] font-bold leading-[1.2] tracking-tight text-white";

/**
 * 그라데이션 글자. 보라 틴트 → 자주 틴트 → 주황. 로고 링의 방향과 같습니다.
 *
 * ADDED 2026-09-17 (8월 문법 브리프). #naru 태그라인 2행이 쓰던 값을 토큰으로
 * 묶고 히어로 H1 2행이 같은 값을 씁니다. 8월 페이지의 2행 그라데이션
 * (violet-300 → fuchsia-300 → cyan-300)은 그쪽 팔레트라 여기 들여오지 않습니다.
 * 8월의 쓰임새(2행만 그라데이션, pb로 디센더 보호)만 같습니다.
 */
// DECIDED 2026-09-18 (감사 반영 브리프 0, 사용자): 주황은 점으로만. 그라데이션은 보라 틴트
// (#A99AD6)에서 자주 틴트(#C79BB4)로 끝납니다. 8월 문법 브리프 3장의 "보라 → 자주 → 주황"을
// 이 결정이 대체합니다. 원색 --purple·--plum이 아니라 틴트인 이유는 대비입니다(어두운 바탕
// 위 원색 보라 2.12:1). globals.css의 단색 폴백(#C79BB4)은 이제 끝 색과 같습니다.
export const GRADIENT_TEXT =
  "gradient-text bg-gradient-to-r from-[#A99AD6] to-[#C79BB4] bg-clip-text pb-[0.14em] text-transparent";
